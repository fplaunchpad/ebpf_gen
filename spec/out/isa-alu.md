<!-- GENERATED by `specgen prose` from ebpf_alu.kspec — DO NOT EDIT.
     Regenerate with `make -C spec prose`; `make -C spec test` fails if this
     file is stale. -->

# The eBPF ALU fragment — generated instruction reference

This document is **generated**, not written. Every statement about an individual instruction below is a structural rendering of that instruction's `sem` / `defined` expression, its encoding record, its recorded side conditions and its `cite` lines, as they appear in the KeelSpec source `ebpf_alu.kspec`. No instruction prose is authored anywhere: the generator (`spec/specgen/lib/emit_prose.ml`) contains one English template per AST *node kind* (§1.2, §1.3) and nothing else. A by-eye comparison of this document against RFC 9669 and `fstar/CONSTRAINTS.md`, including an honest list of the places where this rendering is weaker or noisier than the RFC's own wording, is in `spec/PROSE-CHECK.md`.

| | |
|---|---|
| spec | `ebpf_alu`, version 1 |
| ISA | RFC 9669 (BPF Instruction Set Architecture); pinned kernel Linux 7.0 |
| host | little-endian |
| families | 5 (`alu`, `neg`, `mov`, `movsx`, `swap`) |
| table entries | 19 |
| instructions | 72 |
| excluded forms | 1 |

## 1. How to read this document

### 1.1 What is derived, and what is not

Derived from the spec source: every mnemonic form, encoding, operand read, operation, definedness condition, side condition, exclusion and citation.

Not derived, and therefore the part a reviewer must read critically:

- the **English templates** for the AST node kinds, tabulated in §1.2 and §1.3 — one template per node kind, applied uniformly;
- the **assembly stems** `le` / `be` / `bswap`, which the IL does not carry (its entry names are the `Ebpf.Ast` case names `ToLE` / `ToBE` / `Bswap`); every other mnemonic lowercases its entry name and appends the instance's widths;
- this section, §1.4, and the fixed section headings, which say nothing about any individual instruction.

### 1.2 Rendering table — expressions

Each row is produced by running the generator on the sample term in the first
column, so this table *is* the renderer; it cannot describe it wrongly.
`x`, `y` stand for operand values, `N`, `F`, `NB` for widths.

| `sem` term | pseudocode | English |
|---|---|---|
| `0` | `0` | 0 |
| `x` | `x` | x |
| `N` | `N` | N |
| `(x + y)` | `(x + y)` | the sum of x and y |
| `(x - y)` | `(x - y)` | x minus y |
| `(x * y)` | `(x * y)` | the product of x and y |
| `(x / y)` | `(x / y)` | the unsigned quotient of x divided by y |
| `(sval N (x) / y)` | `(signedN(x) / y)` | the quotient of the signed (two's-complement) interpretation of x as an N-bit value divided by y, rounded toward negative infinity |
| `(x % y)` | `(x % y)` | the unsigned remainder of x divided by y |
| `(sval N (x) % y)` | `(signedN(x) % y)` | the Euclidean remainder of the signed (two's-complement) interpretation of x as an N-bit value divided by y |
| `wrap N (x)` | `(x mod 2^N)` | x, truncated to N bits |
| `wrap N (sval N (x))` | `(signedN(x) mod 2^N)` | the signed (two's-complement) interpretation of x as an N-bit value, reduced modulo 2^N (two's-complement wrap-around) |
| `low N (x)` | `(x mod 2^N)` | the low N bits of x |
| `sval N (x)` | `signedN(x)` | the signed (two's-complement) interpretation of x as an N-bit value |
| `sext F N (x)` | `sext(F, N, x)` | the low F bits of x, sign-extended to N bits |
| `bswap NB (x)` | `bswap(NB, x)` | the low NB bytes of x in reverse order |
| `pow2 (x)` | `2^x` | 2 raised to the power (x) |
| `trunc_div (x) (y)` | `trunc_div(x, y)` | x divided by y, truncated toward zero |
| `trunc_mod (x) (y)` | `trunc_mod(x, y)` | the remainder of x divided by y with the quotient truncated toward zero (the remainder takes the sign of the dividend) |
| `logand N (x) (y)` | `(x & y)` | the N-bit bitwise AND of x and y |
| `logor N (x) (y)` | `(x \| y)` | the N-bit bitwise OR of x and y |
| `logxor N (x) (y)` | `(x ^ y)` | the N-bit bitwise exclusive OR of x and y |
| `if x < y then x else y` | `(x < y) ? x : y` | (if x is less than y, x; otherwise y) |
| `if x = 0 then 0 else y` | `(x != 0) ? y : 0` | (if x is zero, 0; otherwise y) |

### 1.3 Rendering table — conditions

| `defined` term | pseudocode | English |
|---|---|---|
| `true` | `true` | always |
| `x = 0` | `x == 0` | x is zero |
| `x <> 0` | `x != 0` | x is nonzero |
| `x < N` | `x < N` | x is less than N |
| `x <= y` | `x <= y` | x is at most y |
| `x <> 0 && y < N` | `x != 0 && y < N` | x is nonzero and y is less than N |
| `not x = y` | `!(x == y)` | it is not the case that x is equal to y |

### 1.4 Operand reads, immediates and write-back

KeelSpec deliberately does not specify how an operand is *read*; it names the read (`d = dst@n`, `s = src@n`) and leaves the reading functions in the hand-written frame (`Ebpf.Semantics.regbits` / `opbits`, `spec/DESIGN.md` §1). Two consequences for this document:

- a register read at width *n* < 64 is the low *n* bits of the register;
- **the immediate rule is not visible below**: a 32-bit immediate is sign-extended to 64 bits for 64-bit forms and used as a 32-bit pattern for 32-bit forms (`Ebpf.Int.imm64` / `imm32`; `CONSTRAINTS.md` C11). Wherever a section says "`imm` — the 32-bit immediate field, read as a 64-bit operand value", that read is where the sign extension happens.

The write-back direction *is* visible: a result declared `bits n` stored into a 64-bit register is exactly the zero-extension rule, so every section whose result is narrower than 64 bits states it.

### 1.5 Definedness and the two checker modes

Each instruction carries a `defined` condition — the **strict-mode precondition** that a clean-slate checker must prove. That condition is a property of the instruction, and it is all the spec source states. What a *checker* does with an instruction whose precondition it cannot prove is policy that lives outside this document, and it is **not uniform**: the Linux verifier (and the kernel-faithful mode) accepts a *register* divisor that may be zero and a *register* shift amount that may exceed the width — the ISA defines the result, which is the `Operation` line — but **rejects** a zero *immediate* divisor and an out-of-range *immediate* shift outright; strict mode requires the proof in every case, and programs that pass it are safe under the defensive semantics, where the excluded cases are stuck states. See `fstar/CONSTRAINTS.md` C4 and C7 (immediate, both modes) against C5, C6, C8 and C9 (register). Whichever mode is in force, the `Operation` line is the behaviour when the instruction does execute.

### 1.6 Encoding fields written `*`

The spec fixes each of the five encoding fields (`cls`, `opc`, `sbit`, `off`, `imm`) to a byte, or writes `*` for "not fixed by this form — supplied by the instruction's operands". `*` is what makes the disjointness check (K8) conservative: two forms overlap if they *could* decode to the same bytes. Note that the IL does not distinguish "carried by an immediate operand" from "unused by this form": a register-source ALU instruction shows `imm = *` even though it has no immediate operand at all, and the kernel requires such unused fields to be zero. See `spec/PROSE-CHECK.md` (finding P7).

## 2. Instructions

### family `alu` — `Ebpf.Ast.Alu`

- **AST constructor** — `Alu(w, op, dst, src:operand)`
- **Key** — `op : alu_op` (one table row per case)
- **Form axes** — `w : width`, `src : operand`
- **Widths** — `n = bits w`
- **Semantics signature** — `sem (n; d = dst@n, s = src:operand@n) : bits n`
- **Definedness signature** — `defined (n; s)`
- **Instances** — 52
- **Cite** — RFC 9669 sec. 4.1 (arithmetic instructions)
- **Cite** — CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

Width-generic behaviour, one row per table entry (this is what the F* backend
emits as a single `match`):

| entry | `sem` | `defined` | width-generic reading |
|---|---|---|---|
| `alu/ADD` | `wrap n ((d + s))` | `true` | `dst` is set to the sum of the destination value and the source operand value, truncated to n bits. |
| `alu/SUB` | `wrap n ((d - s))` | `true` | `dst` is set to the destination value minus the source operand value, reduced modulo 2^n (two's-complement wrap-around). |
| `alu/MUL` | `wrap n ((d * s))` | `true` | `dst` is set to the product of the destination value and the source operand value, truncated to n bits. |
| `alu/DIV` | `if s = 0 then 0 else wrap n ((d / s))` | `s <> 0` | If the source operand value is zero, `dst` is set to 0; otherwise `dst` is set to the unsigned quotient of the destination value divided by the source operand value, truncated to n bits. |
| `alu/SDIV` | `if s = 0 then 0 else wrap n (trunc_div (sval n (d)) (sval n (s)))` | `s <> 0` | If the source operand value is zero, `dst` is set to 0; otherwise `dst` is set to (the signed (two's-complement) interpretation of the destination value as an n-bit value divided by the signed (two's-complement) interpretation of the source operand value as an n-bit value, truncated toward zero), reduced modulo 2^n (two's-complement wrap-around). |
| `alu/MOD` | `if s = 0 then d else wrap n ((d % s))` | `s <> 0` | If the source operand value is zero, `dst` is set to the destination value; otherwise `dst` is set to the unsigned remainder of the destination value divided by the source operand value, truncated to n bits. |
| `alu/SMOD` | `if s = 0 then d else wrap n (trunc_mod (sval n (d)) (sval n (s)))` | `s <> 0` | If the source operand value is zero, `dst` is set to the destination value; otherwise `dst` is set to the remainder of the signed (two's-complement) interpretation of the destination value as an n-bit value divided by the signed (two's-complement) interpretation of the source operand value as an n-bit value with the quotient truncated toward zero (the remainder takes the sign of the dividend), reduced modulo 2^n (two's-complement wrap-around). |
| `alu/AND` | `logand n (d) (s)` | `true` | `dst` is set to the n-bit bitwise AND of the destination value and the source operand value. |
| `alu/OR` | `logor n (d) (s)` | `true` | `dst` is set to the n-bit bitwise OR of the destination value and the source operand value. |
| `alu/XOR` | `logxor n (d) (s)` | `true` | `dst` is set to the n-bit bitwise exclusive OR of the destination value and the source operand value. |
| `alu/LSH` | `wrap n ((d * pow2 ((s % n))))` | `s < n` | `dst` is set to the product of the destination value and 2 raised to the power (the unsigned remainder of the source operand value divided by n), truncated to n bits. |
| `alu/RSH` | `wrap n ((d / pow2 ((s % n))))` | `s < n` | `dst` is set to the unsigned quotient of the destination value divided by 2 raised to the power (the unsigned remainder of the source operand value divided by n), truncated to n bits. |
| `alu/ARSH` | `wrap n ((sval n (d) / pow2 ((s % n))))` | `s < n` | `dst` is set to (the quotient of the signed (two's-complement) interpretation of the destination value as an n-bit value divided by 2 raised to the power (the unsigned remainder of the source operand value divided by n), rounded toward negative infinity), reduced modulo 2^n (two's-complement wrap-around). |

#### `add32 dst, src` — `alu/ADD/W32/reg`

- **Form** — `add32 dst, src`, i.e. `Alu(W32, ADD, dst, reg)`
- **Encoding** — opcode byte `0x0c` = `cls 0x04` + `opc 0x00` + `sbit 0x08`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 32-bit value (its low 32 bits); `src` — the source register, read as a 32-bit value (its low 32 bits)
- **Writes** — `dst`, a 32-bit result; writing a 32-bit value into the 64-bit destination register clears bits 32..63 (zero extension)

**Operation** — `dst = ((dst + src) mod 2^32)`

`dst` is set to the sum of the destination value and the source register value, truncated to 32 bits.

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Spec source** — `sem: wrap n ((d + s))`, `defined: true` (`ebpf_alu.kspec:58:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `add32 dst, imm` — `alu/ADD/W32/imm`

- **Form** — `add32 dst, imm`, i.e. `Alu(W32, ADD, dst, imm)`
- **Encoding** — opcode byte `0x04` = `cls 0x04` + `opc 0x00` + `sbit 0x00`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 32-bit value (its low 32 bits); `imm` — the 32-bit immediate field, read as a 32-bit operand value (see §1.4)
- **Writes** — `dst`, a 32-bit result; writing a 32-bit value into the 64-bit destination register clears bits 32..63 (zero extension)

**Operation** — `dst = ((dst + imm) mod 2^32)`

`dst` is set to the sum of the destination value and the immediate value, truncated to 32 bits.

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Spec source** — `sem: wrap n ((d + s))`, `defined: true` (`ebpf_alu.kspec:58:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `add64 dst, src` — `alu/ADD/W64/reg`

- **Form** — `add64 dst, src`, i.e. `Alu(W64, ADD, dst, reg)`
- **Encoding** — opcode byte `0x0f` = `cls 0x07` + `opc 0x00` + `sbit 0x08`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 64-bit value; `src` — the source register, read as a 64-bit value
- **Writes** — `dst`, a 64-bit result

**Operation** — `dst = ((dst + src) mod 2^64)`

`dst` is set to the sum of the destination value and the source register value, truncated to 64 bits.

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Spec source** — `sem: wrap n ((d + s))`, `defined: true` (`ebpf_alu.kspec:58:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `add64 dst, imm` — `alu/ADD/W64/imm`

- **Form** — `add64 dst, imm`, i.e. `Alu(W64, ADD, dst, imm)`
- **Encoding** — opcode byte `0x07` = `cls 0x07` + `opc 0x00` + `sbit 0x00`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 64-bit value; `imm` — the 32-bit immediate field, read as a 64-bit operand value (see §1.4)
- **Writes** — `dst`, a 64-bit result

**Operation** — `dst = ((dst + imm) mod 2^64)`

`dst` is set to the sum of the destination value and the immediate value, truncated to 64 bits.

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Spec source** — `sem: wrap n ((d + s))`, `defined: true` (`ebpf_alu.kspec:58:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `sub32 dst, src` — `alu/SUB/W32/reg`

- **Form** — `sub32 dst, src`, i.e. `Alu(W32, SUB, dst, reg)`
- **Encoding** — opcode byte `0x1c` = `cls 0x04` + `opc 0x10` + `sbit 0x08`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 32-bit value (its low 32 bits); `src` — the source register, read as a 32-bit value (its low 32 bits)
- **Writes** — `dst`, a 32-bit result; writing a 32-bit value into the 64-bit destination register clears bits 32..63 (zero extension)

**Operation** — `dst = ((dst - src) mod 2^32)`

`dst` is set to the destination value minus the source register value, reduced modulo 2^32 (two's-complement wrap-around).

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Spec source** — `sem: wrap n ((d - s))`, `defined: true` (`ebpf_alu.kspec:59:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `sub32 dst, imm` — `alu/SUB/W32/imm`

- **Form** — `sub32 dst, imm`, i.e. `Alu(W32, SUB, dst, imm)`
- **Encoding** — opcode byte `0x14` = `cls 0x04` + `opc 0x10` + `sbit 0x00`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 32-bit value (its low 32 bits); `imm` — the 32-bit immediate field, read as a 32-bit operand value (see §1.4)
- **Writes** — `dst`, a 32-bit result; writing a 32-bit value into the 64-bit destination register clears bits 32..63 (zero extension)

**Operation** — `dst = ((dst - imm) mod 2^32)`

`dst` is set to the destination value minus the immediate value, reduced modulo 2^32 (two's-complement wrap-around).

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Spec source** — `sem: wrap n ((d - s))`, `defined: true` (`ebpf_alu.kspec:59:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `sub64 dst, src` — `alu/SUB/W64/reg`

- **Form** — `sub64 dst, src`, i.e. `Alu(W64, SUB, dst, reg)`
- **Encoding** — opcode byte `0x1f` = `cls 0x07` + `opc 0x10` + `sbit 0x08`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 64-bit value; `src` — the source register, read as a 64-bit value
- **Writes** — `dst`, a 64-bit result

**Operation** — `dst = ((dst - src) mod 2^64)`

`dst` is set to the destination value minus the source register value, reduced modulo 2^64 (two's-complement wrap-around).

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Spec source** — `sem: wrap n ((d - s))`, `defined: true` (`ebpf_alu.kspec:59:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `sub64 dst, imm` — `alu/SUB/W64/imm`

- **Form** — `sub64 dst, imm`, i.e. `Alu(W64, SUB, dst, imm)`
- **Encoding** — opcode byte `0x17` = `cls 0x07` + `opc 0x10` + `sbit 0x00`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 64-bit value; `imm` — the 32-bit immediate field, read as a 64-bit operand value (see §1.4)
- **Writes** — `dst`, a 64-bit result

**Operation** — `dst = ((dst - imm) mod 2^64)`

`dst` is set to the destination value minus the immediate value, reduced modulo 2^64 (two's-complement wrap-around).

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Spec source** — `sem: wrap n ((d - s))`, `defined: true` (`ebpf_alu.kspec:59:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `mul32 dst, src` — `alu/MUL/W32/reg`

- **Form** — `mul32 dst, src`, i.e. `Alu(W32, MUL, dst, reg)`
- **Encoding** — opcode byte `0x2c` = `cls 0x04` + `opc 0x20` + `sbit 0x08`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 32-bit value (its low 32 bits); `src` — the source register, read as a 32-bit value (its low 32 bits)
- **Writes** — `dst`, a 32-bit result; writing a 32-bit value into the 64-bit destination register clears bits 32..63 (zero extension)

**Operation** — `dst = ((dst * src) mod 2^32)`

`dst` is set to the product of the destination value and the source register value, truncated to 32 bits.

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Spec source** — `sem: wrap n ((d * s))`, `defined: true` (`ebpf_alu.kspec:60:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `mul32 dst, imm` — `alu/MUL/W32/imm`

- **Form** — `mul32 dst, imm`, i.e. `Alu(W32, MUL, dst, imm)`
- **Encoding** — opcode byte `0x24` = `cls 0x04` + `opc 0x20` + `sbit 0x00`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 32-bit value (its low 32 bits); `imm` — the 32-bit immediate field, read as a 32-bit operand value (see §1.4)
- **Writes** — `dst`, a 32-bit result; writing a 32-bit value into the 64-bit destination register clears bits 32..63 (zero extension)

**Operation** — `dst = ((dst * imm) mod 2^32)`

`dst` is set to the product of the destination value and the immediate value, truncated to 32 bits.

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Spec source** — `sem: wrap n ((d * s))`, `defined: true` (`ebpf_alu.kspec:60:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `mul64 dst, src` — `alu/MUL/W64/reg`

- **Form** — `mul64 dst, src`, i.e. `Alu(W64, MUL, dst, reg)`
- **Encoding** — opcode byte `0x2f` = `cls 0x07` + `opc 0x20` + `sbit 0x08`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 64-bit value; `src` — the source register, read as a 64-bit value
- **Writes** — `dst`, a 64-bit result

**Operation** — `dst = ((dst * src) mod 2^64)`

`dst` is set to the product of the destination value and the source register value, truncated to 64 bits.

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Spec source** — `sem: wrap n ((d * s))`, `defined: true` (`ebpf_alu.kspec:60:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `mul64 dst, imm` — `alu/MUL/W64/imm`

- **Form** — `mul64 dst, imm`, i.e. `Alu(W64, MUL, dst, imm)`
- **Encoding** — opcode byte `0x27` = `cls 0x07` + `opc 0x20` + `sbit 0x00`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 64-bit value; `imm` — the 32-bit immediate field, read as a 64-bit operand value (see §1.4)
- **Writes** — `dst`, a 64-bit result

**Operation** — `dst = ((dst * imm) mod 2^64)`

`dst` is set to the product of the destination value and the immediate value, truncated to 64 bits.

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Spec source** — `sem: wrap n ((d * s))`, `defined: true` (`ebpf_alu.kspec:60:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `div32 dst, src` — `alu/DIV/W32/reg`

- **Form** — `div32 dst, src`, i.e. `Alu(W32, DIV, dst, reg)`
- **Encoding** — opcode byte `0x3c` = `cls 0x04` + `opc 0x30` + `sbit 0x08`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 32-bit value (its low 32 bits); `src` — the source register, read as a 32-bit value (its low 32 bits)
- **Writes** — `dst`, a 32-bit result; writing a 32-bit value into the 64-bit destination register clears bits 32..63 (zero extension)

**Operation** — `dst = (src != 0) ? ((dst / src) mod 2^32) : 0`

If the source register value is zero, `dst` is set to 0; otherwise `dst` is set to the unsigned quotient of the destination value divided by the source register value, truncated to 32 bits.

**Definedness** — `defined: s <> 0`. This is the strict-mode precondition: the source register value is nonzero. The operation above is total regardless — it gives a result for every operand value, including when the condition fails. What a *checker* does with an instruction whose precondition it cannot prove is policy, not semantics: see §1.5.

**Side conditions** (recorded in the IL, discharged on the generated F*):

- `nonzero(s)` — the source register value is nonzero (it is used as a divisor)

**Spec source** — `sem: if s = 0 then 0 else wrap n ((d / s))`, `defined: s <> 0` (`ebpf_alu.kspec:61:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `div32 dst, imm` — `alu/DIV/W32/imm`

- **Form** — `div32 dst, imm`, i.e. `Alu(W32, DIV, dst, imm)`
- **Encoding** — opcode byte `0x34` = `cls 0x04` + `opc 0x30` + `sbit 0x00`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 32-bit value (its low 32 bits); `imm` — the 32-bit immediate field, read as a 32-bit operand value (see §1.4)
- **Writes** — `dst`, a 32-bit result; writing a 32-bit value into the 64-bit destination register clears bits 32..63 (zero extension)

**Operation** — `dst = (imm != 0) ? ((dst / imm) mod 2^32) : 0`

If the immediate value is zero, `dst` is set to 0; otherwise `dst` is set to the unsigned quotient of the destination value divided by the immediate value, truncated to 32 bits.

**Definedness** — `defined: s <> 0`. This is the strict-mode precondition: the immediate value is nonzero. The operation above is total regardless — it gives a result for every operand value, including when the condition fails. What a *checker* does with an instruction whose precondition it cannot prove is policy, not semantics: see §1.5.

**Side conditions** (recorded in the IL, discharged on the generated F*):

- `nonzero(s)` — the immediate value is nonzero (it is used as a divisor)

**Spec source** — `sem: if s = 0 then 0 else wrap n ((d / s))`, `defined: s <> 0` (`ebpf_alu.kspec:61:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `div64 dst, src` — `alu/DIV/W64/reg`

- **Form** — `div64 dst, src`, i.e. `Alu(W64, DIV, dst, reg)`
- **Encoding** — opcode byte `0x3f` = `cls 0x07` + `opc 0x30` + `sbit 0x08`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 64-bit value; `src` — the source register, read as a 64-bit value
- **Writes** — `dst`, a 64-bit result

**Operation** — `dst = (src != 0) ? ((dst / src) mod 2^64) : 0`

If the source register value is zero, `dst` is set to 0; otherwise `dst` is set to the unsigned quotient of the destination value divided by the source register value, truncated to 64 bits.

**Definedness** — `defined: s <> 0`. This is the strict-mode precondition: the source register value is nonzero. The operation above is total regardless — it gives a result for every operand value, including when the condition fails. What a *checker* does with an instruction whose precondition it cannot prove is policy, not semantics: see §1.5.

**Side conditions** (recorded in the IL, discharged on the generated F*):

- `nonzero(s)` — the source register value is nonzero (it is used as a divisor)

**Spec source** — `sem: if s = 0 then 0 else wrap n ((d / s))`, `defined: s <> 0` (`ebpf_alu.kspec:61:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `div64 dst, imm` — `alu/DIV/W64/imm`

- **Form** — `div64 dst, imm`, i.e. `Alu(W64, DIV, dst, imm)`
- **Encoding** — opcode byte `0x37` = `cls 0x07` + `opc 0x30` + `sbit 0x00`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 64-bit value; `imm` — the 32-bit immediate field, read as a 64-bit operand value (see §1.4)
- **Writes** — `dst`, a 64-bit result

**Operation** — `dst = (imm != 0) ? ((dst / imm) mod 2^64) : 0`

If the immediate value is zero, `dst` is set to 0; otherwise `dst` is set to the unsigned quotient of the destination value divided by the immediate value, truncated to 64 bits.

**Definedness** — `defined: s <> 0`. This is the strict-mode precondition: the immediate value is nonzero. The operation above is total regardless — it gives a result for every operand value, including when the condition fails. What a *checker* does with an instruction whose precondition it cannot prove is policy, not semantics: see §1.5.

**Side conditions** (recorded in the IL, discharged on the generated F*):

- `nonzero(s)` — the immediate value is nonzero (it is used as a divisor)

**Spec source** — `sem: if s = 0 then 0 else wrap n ((d / s))`, `defined: s <> 0` (`ebpf_alu.kspec:61:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `sdiv32 dst, src` — `alu/SDIV/W32/reg`

- **Form** — `sdiv32 dst, src`, i.e. `Alu(W32, SDIV, dst, reg)`
- **Encoding** — opcode byte `0x3c` = `cls 0x04` + `opc 0x30` + `sbit 0x08`; `off = 1`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 32-bit value (its low 32 bits); `src` — the source register, read as a 32-bit value (its low 32 bits)
- **Writes** — `dst`, a 32-bit result; writing a 32-bit value into the 64-bit destination register clears bits 32..63 (zero extension)

**Operation** — `dst = (src != 0) ? (trunc_div(signed32(dst), signed32(src)) mod 2^32) : 0`

If the source register value is zero, `dst` is set to 0; otherwise `dst` is set to (the signed (two's-complement) interpretation of the destination value as a 32-bit value divided by the signed (two's-complement) interpretation of the source register value as a 32-bit value, truncated toward zero), reduced modulo 2^32 (two's-complement wrap-around).

**Definedness** — `defined: s <> 0`. This is the strict-mode precondition: the source register value is nonzero. The operation above is total regardless — it gives a result for every operand value, including when the condition fails. What a *checker* does with an instruction whose precondition it cannot prove is policy, not semantics: see §1.5.

**Side conditions** (recorded in the IL, discharged on the generated F*):

- `nonzero(sval n (s))` — the signed (two's-complement) interpretation of the source register value as a 32-bit value is nonzero (it is used as a divisor)

**Spec source** — `sem: if s = 0 then 0 else wrap n (trunc_div (sval n (d)) (sval n (s)))`, `defined: s <> 0` (`ebpf_alu.kspec:62:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `sdiv32 dst, imm` — `alu/SDIV/W32/imm`

- **Form** — `sdiv32 dst, imm`, i.e. `Alu(W32, SDIV, dst, imm)`
- **Encoding** — opcode byte `0x34` = `cls 0x04` + `opc 0x30` + `sbit 0x00`; `off = 1`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 32-bit value (its low 32 bits); `imm` — the 32-bit immediate field, read as a 32-bit operand value (see §1.4)
- **Writes** — `dst`, a 32-bit result; writing a 32-bit value into the 64-bit destination register clears bits 32..63 (zero extension)

**Operation** — `dst = (imm != 0) ? (trunc_div(signed32(dst), signed32(imm)) mod 2^32) : 0`

If the immediate value is zero, `dst` is set to 0; otherwise `dst` is set to (the signed (two's-complement) interpretation of the destination value as a 32-bit value divided by the signed (two's-complement) interpretation of the immediate value as a 32-bit value, truncated toward zero), reduced modulo 2^32 (two's-complement wrap-around).

**Definedness** — `defined: s <> 0`. This is the strict-mode precondition: the immediate value is nonzero. The operation above is total regardless — it gives a result for every operand value, including when the condition fails. What a *checker* does with an instruction whose precondition it cannot prove is policy, not semantics: see §1.5.

**Side conditions** (recorded in the IL, discharged on the generated F*):

- `nonzero(sval n (s))` — the signed (two's-complement) interpretation of the immediate value as a 32-bit value is nonzero (it is used as a divisor)

**Spec source** — `sem: if s = 0 then 0 else wrap n (trunc_div (sval n (d)) (sval n (s)))`, `defined: s <> 0` (`ebpf_alu.kspec:62:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `sdiv64 dst, src` — `alu/SDIV/W64/reg`

- **Form** — `sdiv64 dst, src`, i.e. `Alu(W64, SDIV, dst, reg)`
- **Encoding** — opcode byte `0x3f` = `cls 0x07` + `opc 0x30` + `sbit 0x08`; `off = 1`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 64-bit value; `src` — the source register, read as a 64-bit value
- **Writes** — `dst`, a 64-bit result

**Operation** — `dst = (src != 0) ? (trunc_div(signed64(dst), signed64(src)) mod 2^64) : 0`

If the source register value is zero, `dst` is set to 0; otherwise `dst` is set to (the signed (two's-complement) interpretation of the destination value as a 64-bit value divided by the signed (two's-complement) interpretation of the source register value as a 64-bit value, truncated toward zero), reduced modulo 2^64 (two's-complement wrap-around).

**Definedness** — `defined: s <> 0`. This is the strict-mode precondition: the source register value is nonzero. The operation above is total regardless — it gives a result for every operand value, including when the condition fails. What a *checker* does with an instruction whose precondition it cannot prove is policy, not semantics: see §1.5.

**Side conditions** (recorded in the IL, discharged on the generated F*):

- `nonzero(sval n (s))` — the signed (two's-complement) interpretation of the source register value as a 64-bit value is nonzero (it is used as a divisor)

**Spec source** — `sem: if s = 0 then 0 else wrap n (trunc_div (sval n (d)) (sval n (s)))`, `defined: s <> 0` (`ebpf_alu.kspec:62:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `sdiv64 dst, imm` — `alu/SDIV/W64/imm`

- **Form** — `sdiv64 dst, imm`, i.e. `Alu(W64, SDIV, dst, imm)`
- **Encoding** — opcode byte `0x37` = `cls 0x07` + `opc 0x30` + `sbit 0x00`; `off = 1`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 64-bit value; `imm` — the 32-bit immediate field, read as a 64-bit operand value (see §1.4)
- **Writes** — `dst`, a 64-bit result

**Operation** — `dst = (imm != 0) ? (trunc_div(signed64(dst), signed64(imm)) mod 2^64) : 0`

If the immediate value is zero, `dst` is set to 0; otherwise `dst` is set to (the signed (two's-complement) interpretation of the destination value as a 64-bit value divided by the signed (two's-complement) interpretation of the immediate value as a 64-bit value, truncated toward zero), reduced modulo 2^64 (two's-complement wrap-around).

**Definedness** — `defined: s <> 0`. This is the strict-mode precondition: the immediate value is nonzero. The operation above is total regardless — it gives a result for every operand value, including when the condition fails. What a *checker* does with an instruction whose precondition it cannot prove is policy, not semantics: see §1.5.

**Side conditions** (recorded in the IL, discharged on the generated F*):

- `nonzero(sval n (s))` — the signed (two's-complement) interpretation of the immediate value as a 64-bit value is nonzero (it is used as a divisor)

**Spec source** — `sem: if s = 0 then 0 else wrap n (trunc_div (sval n (d)) (sval n (s)))`, `defined: s <> 0` (`ebpf_alu.kspec:62:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `mod32 dst, src` — `alu/MOD/W32/reg`

- **Form** — `mod32 dst, src`, i.e. `Alu(W32, MOD, dst, reg)`
- **Encoding** — opcode byte `0x9c` = `cls 0x04` + `opc 0x90` + `sbit 0x08`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 32-bit value (its low 32 bits); `src` — the source register, read as a 32-bit value (its low 32 bits)
- **Writes** — `dst`, a 32-bit result; writing a 32-bit value into the 64-bit destination register clears bits 32..63 (zero extension)

**Operation** — `dst = (src != 0) ? ((dst % src) mod 2^32) : dst`

If the source register value is zero, `dst` is set to the destination value; otherwise `dst` is set to the unsigned remainder of the destination value divided by the source register value, truncated to 32 bits.

**Definedness** — `defined: s <> 0`. This is the strict-mode precondition: the source register value is nonzero. The operation above is total regardless — it gives a result for every operand value, including when the condition fails. What a *checker* does with an instruction whose precondition it cannot prove is policy, not semantics: see §1.5.

**Side conditions** (recorded in the IL, discharged on the generated F*):

- `nonzero(s)` — the source register value is nonzero (it is used as a divisor)

**Spec source** — `sem: if s = 0 then d else wrap n ((d % s))`, `defined: s <> 0` (`ebpf_alu.kspec:63:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `mod32 dst, imm` — `alu/MOD/W32/imm`

- **Form** — `mod32 dst, imm`, i.e. `Alu(W32, MOD, dst, imm)`
- **Encoding** — opcode byte `0x94` = `cls 0x04` + `opc 0x90` + `sbit 0x00`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 32-bit value (its low 32 bits); `imm` — the 32-bit immediate field, read as a 32-bit operand value (see §1.4)
- **Writes** — `dst`, a 32-bit result; writing a 32-bit value into the 64-bit destination register clears bits 32..63 (zero extension)

**Operation** — `dst = (imm != 0) ? ((dst % imm) mod 2^32) : dst`

If the immediate value is zero, `dst` is set to the destination value; otherwise `dst` is set to the unsigned remainder of the destination value divided by the immediate value, truncated to 32 bits.

**Definedness** — `defined: s <> 0`. This is the strict-mode precondition: the immediate value is nonzero. The operation above is total regardless — it gives a result for every operand value, including when the condition fails. What a *checker* does with an instruction whose precondition it cannot prove is policy, not semantics: see §1.5.

**Side conditions** (recorded in the IL, discharged on the generated F*):

- `nonzero(s)` — the immediate value is nonzero (it is used as a divisor)

**Spec source** — `sem: if s = 0 then d else wrap n ((d % s))`, `defined: s <> 0` (`ebpf_alu.kspec:63:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `mod64 dst, src` — `alu/MOD/W64/reg`

- **Form** — `mod64 dst, src`, i.e. `Alu(W64, MOD, dst, reg)`
- **Encoding** — opcode byte `0x9f` = `cls 0x07` + `opc 0x90` + `sbit 0x08`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 64-bit value; `src` — the source register, read as a 64-bit value
- **Writes** — `dst`, a 64-bit result

**Operation** — `dst = (src != 0) ? ((dst % src) mod 2^64) : dst`

If the source register value is zero, `dst` is set to the destination value; otherwise `dst` is set to the unsigned remainder of the destination value divided by the source register value, truncated to 64 bits.

**Definedness** — `defined: s <> 0`. This is the strict-mode precondition: the source register value is nonzero. The operation above is total regardless — it gives a result for every operand value, including when the condition fails. What a *checker* does with an instruction whose precondition it cannot prove is policy, not semantics: see §1.5.

**Side conditions** (recorded in the IL, discharged on the generated F*):

- `nonzero(s)` — the source register value is nonzero (it is used as a divisor)

**Spec source** — `sem: if s = 0 then d else wrap n ((d % s))`, `defined: s <> 0` (`ebpf_alu.kspec:63:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `mod64 dst, imm` — `alu/MOD/W64/imm`

- **Form** — `mod64 dst, imm`, i.e. `Alu(W64, MOD, dst, imm)`
- **Encoding** — opcode byte `0x97` = `cls 0x07` + `opc 0x90` + `sbit 0x00`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 64-bit value; `imm` — the 32-bit immediate field, read as a 64-bit operand value (see §1.4)
- **Writes** — `dst`, a 64-bit result

**Operation** — `dst = (imm != 0) ? ((dst % imm) mod 2^64) : dst`

If the immediate value is zero, `dst` is set to the destination value; otherwise `dst` is set to the unsigned remainder of the destination value divided by the immediate value, truncated to 64 bits.

**Definedness** — `defined: s <> 0`. This is the strict-mode precondition: the immediate value is nonzero. The operation above is total regardless — it gives a result for every operand value, including when the condition fails. What a *checker* does with an instruction whose precondition it cannot prove is policy, not semantics: see §1.5.

**Side conditions** (recorded in the IL, discharged on the generated F*):

- `nonzero(s)` — the immediate value is nonzero (it is used as a divisor)

**Spec source** — `sem: if s = 0 then d else wrap n ((d % s))`, `defined: s <> 0` (`ebpf_alu.kspec:63:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `smod32 dst, src` — `alu/SMOD/W32/reg`

- **Form** — `smod32 dst, src`, i.e. `Alu(W32, SMOD, dst, reg)`
- **Encoding** — opcode byte `0x9c` = `cls 0x04` + `opc 0x90` + `sbit 0x08`; `off = 1`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 32-bit value (its low 32 bits); `src` — the source register, read as a 32-bit value (its low 32 bits)
- **Writes** — `dst`, a 32-bit result; writing a 32-bit value into the 64-bit destination register clears bits 32..63 (zero extension)

**Operation** — `dst = (src != 0) ? (trunc_mod(signed32(dst), signed32(src)) mod 2^32) : dst`

If the source register value is zero, `dst` is set to the destination value; otherwise `dst` is set to the remainder of the signed (two's-complement) interpretation of the destination value as a 32-bit value divided by the signed (two's-complement) interpretation of the source register value as a 32-bit value with the quotient truncated toward zero (the remainder takes the sign of the dividend), reduced modulo 2^32 (two's-complement wrap-around).

**Definedness** — `defined: s <> 0`. This is the strict-mode precondition: the source register value is nonzero. The operation above is total regardless — it gives a result for every operand value, including when the condition fails. What a *checker* does with an instruction whose precondition it cannot prove is policy, not semantics: see §1.5.

**Side conditions** (recorded in the IL, discharged on the generated F*):

- `nonzero(sval n (s))` — the signed (two's-complement) interpretation of the source register value as a 32-bit value is nonzero (it is used as a divisor)

**Spec source** — `sem: if s = 0 then d else wrap n (trunc_mod (sval n (d)) (sval n (s)))`, `defined: s <> 0` (`ebpf_alu.kspec:64:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `smod32 dst, imm` — `alu/SMOD/W32/imm`

- **Form** — `smod32 dst, imm`, i.e. `Alu(W32, SMOD, dst, imm)`
- **Encoding** — opcode byte `0x94` = `cls 0x04` + `opc 0x90` + `sbit 0x00`; `off = 1`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 32-bit value (its low 32 bits); `imm` — the 32-bit immediate field, read as a 32-bit operand value (see §1.4)
- **Writes** — `dst`, a 32-bit result; writing a 32-bit value into the 64-bit destination register clears bits 32..63 (zero extension)

**Operation** — `dst = (imm != 0) ? (trunc_mod(signed32(dst), signed32(imm)) mod 2^32) : dst`

If the immediate value is zero, `dst` is set to the destination value; otherwise `dst` is set to the remainder of the signed (two's-complement) interpretation of the destination value as a 32-bit value divided by the signed (two's-complement) interpretation of the immediate value as a 32-bit value with the quotient truncated toward zero (the remainder takes the sign of the dividend), reduced modulo 2^32 (two's-complement wrap-around).

**Definedness** — `defined: s <> 0`. This is the strict-mode precondition: the immediate value is nonzero. The operation above is total regardless — it gives a result for every operand value, including when the condition fails. What a *checker* does with an instruction whose precondition it cannot prove is policy, not semantics: see §1.5.

**Side conditions** (recorded in the IL, discharged on the generated F*):

- `nonzero(sval n (s))` — the signed (two's-complement) interpretation of the immediate value as a 32-bit value is nonzero (it is used as a divisor)

**Spec source** — `sem: if s = 0 then d else wrap n (trunc_mod (sval n (d)) (sval n (s)))`, `defined: s <> 0` (`ebpf_alu.kspec:64:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `smod64 dst, src` — `alu/SMOD/W64/reg`

- **Form** — `smod64 dst, src`, i.e. `Alu(W64, SMOD, dst, reg)`
- **Encoding** — opcode byte `0x9f` = `cls 0x07` + `opc 0x90` + `sbit 0x08`; `off = 1`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 64-bit value; `src` — the source register, read as a 64-bit value
- **Writes** — `dst`, a 64-bit result

**Operation** — `dst = (src != 0) ? (trunc_mod(signed64(dst), signed64(src)) mod 2^64) : dst`

If the source register value is zero, `dst` is set to the destination value; otherwise `dst` is set to the remainder of the signed (two's-complement) interpretation of the destination value as a 64-bit value divided by the signed (two's-complement) interpretation of the source register value as a 64-bit value with the quotient truncated toward zero (the remainder takes the sign of the dividend), reduced modulo 2^64 (two's-complement wrap-around).

**Definedness** — `defined: s <> 0`. This is the strict-mode precondition: the source register value is nonzero. The operation above is total regardless — it gives a result for every operand value, including when the condition fails. What a *checker* does with an instruction whose precondition it cannot prove is policy, not semantics: see §1.5.

**Side conditions** (recorded in the IL, discharged on the generated F*):

- `nonzero(sval n (s))` — the signed (two's-complement) interpretation of the source register value as a 64-bit value is nonzero (it is used as a divisor)

**Spec source** — `sem: if s = 0 then d else wrap n (trunc_mod (sval n (d)) (sval n (s)))`, `defined: s <> 0` (`ebpf_alu.kspec:64:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `smod64 dst, imm` — `alu/SMOD/W64/imm`

- **Form** — `smod64 dst, imm`, i.e. `Alu(W64, SMOD, dst, imm)`
- **Encoding** — opcode byte `0x97` = `cls 0x07` + `opc 0x90` + `sbit 0x00`; `off = 1`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 64-bit value; `imm` — the 32-bit immediate field, read as a 64-bit operand value (see §1.4)
- **Writes** — `dst`, a 64-bit result

**Operation** — `dst = (imm != 0) ? (trunc_mod(signed64(dst), signed64(imm)) mod 2^64) : dst`

If the immediate value is zero, `dst` is set to the destination value; otherwise `dst` is set to the remainder of the signed (two's-complement) interpretation of the destination value as a 64-bit value divided by the signed (two's-complement) interpretation of the immediate value as a 64-bit value with the quotient truncated toward zero (the remainder takes the sign of the dividend), reduced modulo 2^64 (two's-complement wrap-around).

**Definedness** — `defined: s <> 0`. This is the strict-mode precondition: the immediate value is nonzero. The operation above is total regardless — it gives a result for every operand value, including when the condition fails. What a *checker* does with an instruction whose precondition it cannot prove is policy, not semantics: see §1.5.

**Side conditions** (recorded in the IL, discharged on the generated F*):

- `nonzero(sval n (s))` — the signed (two's-complement) interpretation of the immediate value as a 64-bit value is nonzero (it is used as a divisor)

**Spec source** — `sem: if s = 0 then d else wrap n (trunc_mod (sval n (d)) (sval n (s)))`, `defined: s <> 0` (`ebpf_alu.kspec:64:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `and32 dst, src` — `alu/AND/W32/reg`

- **Form** — `and32 dst, src`, i.e. `Alu(W32, AND, dst, reg)`
- **Encoding** — opcode byte `0x5c` = `cls 0x04` + `opc 0x50` + `sbit 0x08`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 32-bit value (its low 32 bits); `src` — the source register, read as a 32-bit value (its low 32 bits)
- **Writes** — `dst`, a 32-bit result; writing a 32-bit value into the 64-bit destination register clears bits 32..63 (zero extension)

**Operation** — `dst = (dst & src)`

`dst` is set to the 32-bit bitwise AND of the destination value and the source register value.

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Spec source** — `sem: logand n (d) (s)`, `defined: true` (`ebpf_alu.kspec:65:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `and32 dst, imm` — `alu/AND/W32/imm`

- **Form** — `and32 dst, imm`, i.e. `Alu(W32, AND, dst, imm)`
- **Encoding** — opcode byte `0x54` = `cls 0x04` + `opc 0x50` + `sbit 0x00`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 32-bit value (its low 32 bits); `imm` — the 32-bit immediate field, read as a 32-bit operand value (see §1.4)
- **Writes** — `dst`, a 32-bit result; writing a 32-bit value into the 64-bit destination register clears bits 32..63 (zero extension)

**Operation** — `dst = (dst & imm)`

`dst` is set to the 32-bit bitwise AND of the destination value and the immediate value.

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Spec source** — `sem: logand n (d) (s)`, `defined: true` (`ebpf_alu.kspec:65:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `and64 dst, src` — `alu/AND/W64/reg`

- **Form** — `and64 dst, src`, i.e. `Alu(W64, AND, dst, reg)`
- **Encoding** — opcode byte `0x5f` = `cls 0x07` + `opc 0x50` + `sbit 0x08`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 64-bit value; `src` — the source register, read as a 64-bit value
- **Writes** — `dst`, a 64-bit result

**Operation** — `dst = (dst & src)`

`dst` is set to the 64-bit bitwise AND of the destination value and the source register value.

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Spec source** — `sem: logand n (d) (s)`, `defined: true` (`ebpf_alu.kspec:65:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `and64 dst, imm` — `alu/AND/W64/imm`

- **Form** — `and64 dst, imm`, i.e. `Alu(W64, AND, dst, imm)`
- **Encoding** — opcode byte `0x57` = `cls 0x07` + `opc 0x50` + `sbit 0x00`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 64-bit value; `imm` — the 32-bit immediate field, read as a 64-bit operand value (see §1.4)
- **Writes** — `dst`, a 64-bit result

**Operation** — `dst = (dst & imm)`

`dst` is set to the 64-bit bitwise AND of the destination value and the immediate value.

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Spec source** — `sem: logand n (d) (s)`, `defined: true` (`ebpf_alu.kspec:65:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `or32 dst, src` — `alu/OR/W32/reg`

- **Form** — `or32 dst, src`, i.e. `Alu(W32, OR, dst, reg)`
- **Encoding** — opcode byte `0x4c` = `cls 0x04` + `opc 0x40` + `sbit 0x08`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 32-bit value (its low 32 bits); `src` — the source register, read as a 32-bit value (its low 32 bits)
- **Writes** — `dst`, a 32-bit result; writing a 32-bit value into the 64-bit destination register clears bits 32..63 (zero extension)

**Operation** — `dst = (dst | src)`

`dst` is set to the 32-bit bitwise OR of the destination value and the source register value.

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Spec source** — `sem: logor n (d) (s)`, `defined: true` (`ebpf_alu.kspec:66:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `or32 dst, imm` — `alu/OR/W32/imm`

- **Form** — `or32 dst, imm`, i.e. `Alu(W32, OR, dst, imm)`
- **Encoding** — opcode byte `0x44` = `cls 0x04` + `opc 0x40` + `sbit 0x00`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 32-bit value (its low 32 bits); `imm` — the 32-bit immediate field, read as a 32-bit operand value (see §1.4)
- **Writes** — `dst`, a 32-bit result; writing a 32-bit value into the 64-bit destination register clears bits 32..63 (zero extension)

**Operation** — `dst = (dst | imm)`

`dst` is set to the 32-bit bitwise OR of the destination value and the immediate value.

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Spec source** — `sem: logor n (d) (s)`, `defined: true` (`ebpf_alu.kspec:66:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `or64 dst, src` — `alu/OR/W64/reg`

- **Form** — `or64 dst, src`, i.e. `Alu(W64, OR, dst, reg)`
- **Encoding** — opcode byte `0x4f` = `cls 0x07` + `opc 0x40` + `sbit 0x08`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 64-bit value; `src` — the source register, read as a 64-bit value
- **Writes** — `dst`, a 64-bit result

**Operation** — `dst = (dst | src)`

`dst` is set to the 64-bit bitwise OR of the destination value and the source register value.

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Spec source** — `sem: logor n (d) (s)`, `defined: true` (`ebpf_alu.kspec:66:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `or64 dst, imm` — `alu/OR/W64/imm`

- **Form** — `or64 dst, imm`, i.e. `Alu(W64, OR, dst, imm)`
- **Encoding** — opcode byte `0x47` = `cls 0x07` + `opc 0x40` + `sbit 0x00`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 64-bit value; `imm` — the 32-bit immediate field, read as a 64-bit operand value (see §1.4)
- **Writes** — `dst`, a 64-bit result

**Operation** — `dst = (dst | imm)`

`dst` is set to the 64-bit bitwise OR of the destination value and the immediate value.

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Spec source** — `sem: logor n (d) (s)`, `defined: true` (`ebpf_alu.kspec:66:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `xor32 dst, src` — `alu/XOR/W32/reg`

- **Form** — `xor32 dst, src`, i.e. `Alu(W32, XOR, dst, reg)`
- **Encoding** — opcode byte `0xac` = `cls 0x04` + `opc 0xa0` + `sbit 0x08`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 32-bit value (its low 32 bits); `src` — the source register, read as a 32-bit value (its low 32 bits)
- **Writes** — `dst`, a 32-bit result; writing a 32-bit value into the 64-bit destination register clears bits 32..63 (zero extension)

**Operation** — `dst = (dst ^ src)`

`dst` is set to the 32-bit bitwise exclusive OR of the destination value and the source register value.

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Spec source** — `sem: logxor n (d) (s)`, `defined: true` (`ebpf_alu.kspec:67:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `xor32 dst, imm` — `alu/XOR/W32/imm`

- **Form** — `xor32 dst, imm`, i.e. `Alu(W32, XOR, dst, imm)`
- **Encoding** — opcode byte `0xa4` = `cls 0x04` + `opc 0xa0` + `sbit 0x00`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 32-bit value (its low 32 bits); `imm` — the 32-bit immediate field, read as a 32-bit operand value (see §1.4)
- **Writes** — `dst`, a 32-bit result; writing a 32-bit value into the 64-bit destination register clears bits 32..63 (zero extension)

**Operation** — `dst = (dst ^ imm)`

`dst` is set to the 32-bit bitwise exclusive OR of the destination value and the immediate value.

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Spec source** — `sem: logxor n (d) (s)`, `defined: true` (`ebpf_alu.kspec:67:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `xor64 dst, src` — `alu/XOR/W64/reg`

- **Form** — `xor64 dst, src`, i.e. `Alu(W64, XOR, dst, reg)`
- **Encoding** — opcode byte `0xaf` = `cls 0x07` + `opc 0xa0` + `sbit 0x08`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 64-bit value; `src` — the source register, read as a 64-bit value
- **Writes** — `dst`, a 64-bit result

**Operation** — `dst = (dst ^ src)`

`dst` is set to the 64-bit bitwise exclusive OR of the destination value and the source register value.

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Spec source** — `sem: logxor n (d) (s)`, `defined: true` (`ebpf_alu.kspec:67:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `xor64 dst, imm` — `alu/XOR/W64/imm`

- **Form** — `xor64 dst, imm`, i.e. `Alu(W64, XOR, dst, imm)`
- **Encoding** — opcode byte `0xa7` = `cls 0x07` + `opc 0xa0` + `sbit 0x00`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 64-bit value; `imm` — the 32-bit immediate field, read as a 64-bit operand value (see §1.4)
- **Writes** — `dst`, a 64-bit result

**Operation** — `dst = (dst ^ imm)`

`dst` is set to the 64-bit bitwise exclusive OR of the destination value and the immediate value.

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Spec source** — `sem: logxor n (d) (s)`, `defined: true` (`ebpf_alu.kspec:67:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `lsh32 dst, src` — `alu/LSH/W32/reg`

- **Form** — `lsh32 dst, src`, i.e. `Alu(W32, LSH, dst, reg)`
- **Encoding** — opcode byte `0x6c` = `cls 0x04` + `opc 0x60` + `sbit 0x08`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 32-bit value (its low 32 bits); `src` — the source register, read as a 32-bit value (its low 32 bits)
- **Writes** — `dst`, a 32-bit result; writing a 32-bit value into the 64-bit destination register clears bits 32..63 (zero extension)

**Operation** — `dst = ((dst * 2^(src % 32)) mod 2^32)`

`dst` is set to the product of the destination value and 2 raised to the power (the unsigned remainder of the source register value divided by 32), truncated to 32 bits.

**Definedness** — `defined: s < n`. This is the strict-mode precondition: the source register value is less than 32. The operation above is total regardless — it gives a result for every operand value, including when the condition fails. What a *checker* does with an instruction whose precondition it cannot prove is policy, not semantics: see §1.5.

**Side conditions** (recorded in the IL, discharged on the generated F*):

- `nonzero(n)` — 32 is nonzero (it is used as a divisor)
- `nonneg((s % n))` — the unsigned remainder of the source register value divided by 32 is non-negative (it is a `pow2` exponent)

**Spec source** — `sem: wrap n ((d * pow2 ((s % n))))`, `defined: s < n` (`ebpf_alu.kspec:68:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `lsh32 dst, imm` — `alu/LSH/W32/imm`

- **Form** — `lsh32 dst, imm`, i.e. `Alu(W32, LSH, dst, imm)`
- **Encoding** — opcode byte `0x64` = `cls 0x04` + `opc 0x60` + `sbit 0x00`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 32-bit value (its low 32 bits); `imm` — the 32-bit immediate field, read as a 32-bit operand value (see §1.4)
- **Writes** — `dst`, a 32-bit result; writing a 32-bit value into the 64-bit destination register clears bits 32..63 (zero extension)

**Operation** — `dst = ((dst * 2^(imm % 32)) mod 2^32)`

`dst` is set to the product of the destination value and 2 raised to the power (the unsigned remainder of the immediate value divided by 32), truncated to 32 bits.

**Definedness** — `defined: s < n`. This is the strict-mode precondition: the immediate value is less than 32. The operation above is total regardless — it gives a result for every operand value, including when the condition fails. What a *checker* does with an instruction whose precondition it cannot prove is policy, not semantics: see §1.5.

**Side conditions** (recorded in the IL, discharged on the generated F*):

- `nonzero(n)` — 32 is nonzero (it is used as a divisor)
- `nonneg((s % n))` — the unsigned remainder of the immediate value divided by 32 is non-negative (it is a `pow2` exponent)

**Spec source** — `sem: wrap n ((d * pow2 ((s % n))))`, `defined: s < n` (`ebpf_alu.kspec:68:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `lsh64 dst, src` — `alu/LSH/W64/reg`

- **Form** — `lsh64 dst, src`, i.e. `Alu(W64, LSH, dst, reg)`
- **Encoding** — opcode byte `0x6f` = `cls 0x07` + `opc 0x60` + `sbit 0x08`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 64-bit value; `src` — the source register, read as a 64-bit value
- **Writes** — `dst`, a 64-bit result

**Operation** — `dst = ((dst * 2^(src % 64)) mod 2^64)`

`dst` is set to the product of the destination value and 2 raised to the power (the unsigned remainder of the source register value divided by 64), truncated to 64 bits.

**Definedness** — `defined: s < n`. This is the strict-mode precondition: the source register value is less than 64. The operation above is total regardless — it gives a result for every operand value, including when the condition fails. What a *checker* does with an instruction whose precondition it cannot prove is policy, not semantics: see §1.5.

**Side conditions** (recorded in the IL, discharged on the generated F*):

- `nonzero(n)` — 64 is nonzero (it is used as a divisor)
- `nonneg((s % n))` — the unsigned remainder of the source register value divided by 64 is non-negative (it is a `pow2` exponent)

**Spec source** — `sem: wrap n ((d * pow2 ((s % n))))`, `defined: s < n` (`ebpf_alu.kspec:68:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `lsh64 dst, imm` — `alu/LSH/W64/imm`

- **Form** — `lsh64 dst, imm`, i.e. `Alu(W64, LSH, dst, imm)`
- **Encoding** — opcode byte `0x67` = `cls 0x07` + `opc 0x60` + `sbit 0x00`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 64-bit value; `imm` — the 32-bit immediate field, read as a 64-bit operand value (see §1.4)
- **Writes** — `dst`, a 64-bit result

**Operation** — `dst = ((dst * 2^(imm % 64)) mod 2^64)`

`dst` is set to the product of the destination value and 2 raised to the power (the unsigned remainder of the immediate value divided by 64), truncated to 64 bits.

**Definedness** — `defined: s < n`. This is the strict-mode precondition: the immediate value is less than 64. The operation above is total regardless — it gives a result for every operand value, including when the condition fails. What a *checker* does with an instruction whose precondition it cannot prove is policy, not semantics: see §1.5.

**Side conditions** (recorded in the IL, discharged on the generated F*):

- `nonzero(n)` — 64 is nonzero (it is used as a divisor)
- `nonneg((s % n))` — the unsigned remainder of the immediate value divided by 64 is non-negative (it is a `pow2` exponent)

**Spec source** — `sem: wrap n ((d * pow2 ((s % n))))`, `defined: s < n` (`ebpf_alu.kspec:68:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `rsh32 dst, src` — `alu/RSH/W32/reg`

- **Form** — `rsh32 dst, src`, i.e. `Alu(W32, RSH, dst, reg)`
- **Encoding** — opcode byte `0x7c` = `cls 0x04` + `opc 0x70` + `sbit 0x08`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 32-bit value (its low 32 bits); `src` — the source register, read as a 32-bit value (its low 32 bits)
- **Writes** — `dst`, a 32-bit result; writing a 32-bit value into the 64-bit destination register clears bits 32..63 (zero extension)

**Operation** — `dst = ((dst / 2^(src % 32)) mod 2^32)`

`dst` is set to the unsigned quotient of the destination value divided by 2 raised to the power (the unsigned remainder of the source register value divided by 32), truncated to 32 bits.

**Definedness** — `defined: s < n`. This is the strict-mode precondition: the source register value is less than 32. The operation above is total regardless — it gives a result for every operand value, including when the condition fails. What a *checker* does with an instruction whose precondition it cannot prove is policy, not semantics: see §1.5.

**Side conditions** (recorded in the IL, discharged on the generated F*):

- `nonzero(n)` — 32 is nonzero (it is used as a divisor)
- `nonneg((s % n))` — the unsigned remainder of the source register value divided by 32 is non-negative (it is a `pow2` exponent)
- `nonzero(pow2 ((s % n)))` — 2 raised to the power (the unsigned remainder of the source register value divided by 32) is nonzero (it is used as a divisor)

**Spec source** — `sem: wrap n ((d / pow2 ((s % n))))`, `defined: s < n` (`ebpf_alu.kspec:69:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `rsh32 dst, imm` — `alu/RSH/W32/imm`

- **Form** — `rsh32 dst, imm`, i.e. `Alu(W32, RSH, dst, imm)`
- **Encoding** — opcode byte `0x74` = `cls 0x04` + `opc 0x70` + `sbit 0x00`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 32-bit value (its low 32 bits); `imm` — the 32-bit immediate field, read as a 32-bit operand value (see §1.4)
- **Writes** — `dst`, a 32-bit result; writing a 32-bit value into the 64-bit destination register clears bits 32..63 (zero extension)

**Operation** — `dst = ((dst / 2^(imm % 32)) mod 2^32)`

`dst` is set to the unsigned quotient of the destination value divided by 2 raised to the power (the unsigned remainder of the immediate value divided by 32), truncated to 32 bits.

**Definedness** — `defined: s < n`. This is the strict-mode precondition: the immediate value is less than 32. The operation above is total regardless — it gives a result for every operand value, including when the condition fails. What a *checker* does with an instruction whose precondition it cannot prove is policy, not semantics: see §1.5.

**Side conditions** (recorded in the IL, discharged on the generated F*):

- `nonzero(n)` — 32 is nonzero (it is used as a divisor)
- `nonneg((s % n))` — the unsigned remainder of the immediate value divided by 32 is non-negative (it is a `pow2` exponent)
- `nonzero(pow2 ((s % n)))` — 2 raised to the power (the unsigned remainder of the immediate value divided by 32) is nonzero (it is used as a divisor)

**Spec source** — `sem: wrap n ((d / pow2 ((s % n))))`, `defined: s < n` (`ebpf_alu.kspec:69:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `rsh64 dst, src` — `alu/RSH/W64/reg`

- **Form** — `rsh64 dst, src`, i.e. `Alu(W64, RSH, dst, reg)`
- **Encoding** — opcode byte `0x7f` = `cls 0x07` + `opc 0x70` + `sbit 0x08`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 64-bit value; `src` — the source register, read as a 64-bit value
- **Writes** — `dst`, a 64-bit result

**Operation** — `dst = ((dst / 2^(src % 64)) mod 2^64)`

`dst` is set to the unsigned quotient of the destination value divided by 2 raised to the power (the unsigned remainder of the source register value divided by 64), truncated to 64 bits.

**Definedness** — `defined: s < n`. This is the strict-mode precondition: the source register value is less than 64. The operation above is total regardless — it gives a result for every operand value, including when the condition fails. What a *checker* does with an instruction whose precondition it cannot prove is policy, not semantics: see §1.5.

**Side conditions** (recorded in the IL, discharged on the generated F*):

- `nonzero(n)` — 64 is nonzero (it is used as a divisor)
- `nonneg((s % n))` — the unsigned remainder of the source register value divided by 64 is non-negative (it is a `pow2` exponent)
- `nonzero(pow2 ((s % n)))` — 2 raised to the power (the unsigned remainder of the source register value divided by 64) is nonzero (it is used as a divisor)

**Spec source** — `sem: wrap n ((d / pow2 ((s % n))))`, `defined: s < n` (`ebpf_alu.kspec:69:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `rsh64 dst, imm` — `alu/RSH/W64/imm`

- **Form** — `rsh64 dst, imm`, i.e. `Alu(W64, RSH, dst, imm)`
- **Encoding** — opcode byte `0x77` = `cls 0x07` + `opc 0x70` + `sbit 0x00`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 64-bit value; `imm` — the 32-bit immediate field, read as a 64-bit operand value (see §1.4)
- **Writes** — `dst`, a 64-bit result

**Operation** — `dst = ((dst / 2^(imm % 64)) mod 2^64)`

`dst` is set to the unsigned quotient of the destination value divided by 2 raised to the power (the unsigned remainder of the immediate value divided by 64), truncated to 64 bits.

**Definedness** — `defined: s < n`. This is the strict-mode precondition: the immediate value is less than 64. The operation above is total regardless — it gives a result for every operand value, including when the condition fails. What a *checker* does with an instruction whose precondition it cannot prove is policy, not semantics: see §1.5.

**Side conditions** (recorded in the IL, discharged on the generated F*):

- `nonzero(n)` — 64 is nonzero (it is used as a divisor)
- `nonneg((s % n))` — the unsigned remainder of the immediate value divided by 64 is non-negative (it is a `pow2` exponent)
- `nonzero(pow2 ((s % n)))` — 2 raised to the power (the unsigned remainder of the immediate value divided by 64) is nonzero (it is used as a divisor)

**Spec source** — `sem: wrap n ((d / pow2 ((s % n))))`, `defined: s < n` (`ebpf_alu.kspec:69:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `arsh32 dst, src` — `alu/ARSH/W32/reg`

- **Form** — `arsh32 dst, src`, i.e. `Alu(W32, ARSH, dst, reg)`
- **Encoding** — opcode byte `0xcc` = `cls 0x04` + `opc 0xc0` + `sbit 0x08`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 32-bit value (its low 32 bits); `src` — the source register, read as a 32-bit value (its low 32 bits)
- **Writes** — `dst`, a 32-bit result; writing a 32-bit value into the 64-bit destination register clears bits 32..63 (zero extension)

**Operation** — `dst = ((signed32(dst) / 2^(src % 32)) mod 2^32)`

`dst` is set to (the quotient of the signed (two's-complement) interpretation of the destination value as a 32-bit value divided by 2 raised to the power (the unsigned remainder of the source register value divided by 32), rounded toward negative infinity), reduced modulo 2^32 (two's-complement wrap-around).

**Definedness** — `defined: s < n`. This is the strict-mode precondition: the source register value is less than 32. The operation above is total regardless — it gives a result for every operand value, including when the condition fails. What a *checker* does with an instruction whose precondition it cannot prove is policy, not semantics: see §1.5.

**Side conditions** (recorded in the IL, discharged on the generated F*):

- `nonzero(n)` — 32 is nonzero (it is used as a divisor)
- `nonneg((s % n))` — the unsigned remainder of the source register value divided by 32 is non-negative (it is a `pow2` exponent)
- `nonzero(pow2 ((s % n)))` — 2 raised to the power (the unsigned remainder of the source register value divided by 32) is nonzero (it is used as a divisor)

**Spec source** — `sem: wrap n ((sval n (d) / pow2 ((s % n))))`, `defined: s < n` (`ebpf_alu.kspec:70:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `arsh32 dst, imm` — `alu/ARSH/W32/imm`

- **Form** — `arsh32 dst, imm`, i.e. `Alu(W32, ARSH, dst, imm)`
- **Encoding** — opcode byte `0xc4` = `cls 0x04` + `opc 0xc0` + `sbit 0x00`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 32-bit value (its low 32 bits); `imm` — the 32-bit immediate field, read as a 32-bit operand value (see §1.4)
- **Writes** — `dst`, a 32-bit result; writing a 32-bit value into the 64-bit destination register clears bits 32..63 (zero extension)

**Operation** — `dst = ((signed32(dst) / 2^(imm % 32)) mod 2^32)`

`dst` is set to (the quotient of the signed (two's-complement) interpretation of the destination value as a 32-bit value divided by 2 raised to the power (the unsigned remainder of the immediate value divided by 32), rounded toward negative infinity), reduced modulo 2^32 (two's-complement wrap-around).

**Definedness** — `defined: s < n`. This is the strict-mode precondition: the immediate value is less than 32. The operation above is total regardless — it gives a result for every operand value, including when the condition fails. What a *checker* does with an instruction whose precondition it cannot prove is policy, not semantics: see §1.5.

**Side conditions** (recorded in the IL, discharged on the generated F*):

- `nonzero(n)` — 32 is nonzero (it is used as a divisor)
- `nonneg((s % n))` — the unsigned remainder of the immediate value divided by 32 is non-negative (it is a `pow2` exponent)
- `nonzero(pow2 ((s % n)))` — 2 raised to the power (the unsigned remainder of the immediate value divided by 32) is nonzero (it is used as a divisor)

**Spec source** — `sem: wrap n ((sval n (d) / pow2 ((s % n))))`, `defined: s < n` (`ebpf_alu.kspec:70:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `arsh64 dst, src` — `alu/ARSH/W64/reg`

- **Form** — `arsh64 dst, src`, i.e. `Alu(W64, ARSH, dst, reg)`
- **Encoding** — opcode byte `0xcf` = `cls 0x07` + `opc 0xc0` + `sbit 0x08`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 64-bit value; `src` — the source register, read as a 64-bit value
- **Writes** — `dst`, a 64-bit result

**Operation** — `dst = ((signed64(dst) / 2^(src % 64)) mod 2^64)`

`dst` is set to (the quotient of the signed (two's-complement) interpretation of the destination value as a 64-bit value divided by 2 raised to the power (the unsigned remainder of the source register value divided by 64), rounded toward negative infinity), reduced modulo 2^64 (two's-complement wrap-around).

**Definedness** — `defined: s < n`. This is the strict-mode precondition: the source register value is less than 64. The operation above is total regardless — it gives a result for every operand value, including when the condition fails. What a *checker* does with an instruction whose precondition it cannot prove is policy, not semantics: see §1.5.

**Side conditions** (recorded in the IL, discharged on the generated F*):

- `nonzero(n)` — 64 is nonzero (it is used as a divisor)
- `nonneg((s % n))` — the unsigned remainder of the source register value divided by 64 is non-negative (it is a `pow2` exponent)
- `nonzero(pow2 ((s % n)))` — 2 raised to the power (the unsigned remainder of the source register value divided by 64) is nonzero (it is used as a divisor)

**Spec source** — `sem: wrap n ((sval n (d) / pow2 ((s % n))))`, `defined: s < n` (`ebpf_alu.kspec:70:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

#### `arsh64 dst, imm` — `alu/ARSH/W64/imm`

- **Form** — `arsh64 dst, imm`, i.e. `Alu(W64, ARSH, dst, imm)`
- **Encoding** — opcode byte `0xc7` = `cls 0x07` + `opc 0xc0` + `sbit 0x00`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 64-bit value; `imm` — the 32-bit immediate field, read as a 64-bit operand value (see §1.4)
- **Writes** — `dst`, a 64-bit result

**Operation** — `dst = ((signed64(dst) / 2^(imm % 64)) mod 2^64)`

`dst` is set to (the quotient of the signed (two's-complement) interpretation of the destination value as a 64-bit value divided by 2 raised to the power (the unsigned remainder of the immediate value divided by 64), rounded toward negative infinity), reduced modulo 2^64 (two's-complement wrap-around).

**Definedness** — `defined: s < n`. This is the strict-mode precondition: the immediate value is less than 64. The operation above is total regardless — it gives a result for every operand value, including when the condition fails. What a *checker* does with an instruction whose precondition it cannot prove is policy, not semantics: see §1.5.

**Side conditions** (recorded in the IL, discharged on the generated F*):

- `nonzero(n)` — 64 is nonzero (it is used as a divisor)
- `nonneg((s % n))` — the unsigned remainder of the immediate value divided by 64 is non-negative (it is a `pow2` exponent)
- `nonzero(pow2 ((s % n)))` — 2 raised to the power (the unsigned remainder of the immediate value divided by 64) is nonzero (it is used as a divisor)

**Spec source** — `sem: wrap n ((sval n (d) / pow2 ((s % n))))`, `defined: s < n` (`ebpf_alu.kspec:70:3`)

**Provenance** — RFC 9669 sec. 4.1 (arithmetic instructions); CONSTRAINTS.md C5, C6, C7, C8, C9, C10, C11, C12

### family `neg` — `Ebpf.Ast.Neg`

- **AST constructor** — `Neg(w, dst)`
- **Form axes** — `w : width`
- **Widths** — `n = bits w`
- **Semantics signature** — `sem (n; d = dst@n) : bits n`
- **Definedness signature** — `defined (n)`
- **Instances** — 2
- **Cite** — RFC 9669 sec. 4.1 (NEG)

Width-generic behaviour, one row per table entry (this is what the F* backend
emits as a single `match`):

| entry | `sem` | `defined` | width-generic reading |
|---|---|---|---|
| `neg/NEG` | `wrap n ((0 - d))` | `true` | `dst` is set to 0 minus the destination value, reduced modulo 2^n (two's-complement wrap-around). |

#### `neg32 dst` — `neg/NEG/W32`

- **Form** — `neg32 dst`, i.e. `Neg(W32, dst)`
- **Encoding** — opcode byte `0x84` = `cls 0x04` + `opc 0x80` + `sbit 0x00`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 32-bit value (its low 32 bits)
- **Writes** — `dst`, a 32-bit result; writing a 32-bit value into the 64-bit destination register clears bits 32..63 (zero extension)

**Operation** — `dst = ((0 - dst) mod 2^32)`

`dst` is set to 0 minus the destination value, reduced modulo 2^32 (two's-complement wrap-around).

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Spec source** — `sem: wrap n ((0 - d))`, `defined: true` (`ebpf_alu.kspec:85:3`)

**Provenance** — RFC 9669 sec. 4.1 (NEG)

#### `neg64 dst` — `neg/NEG/W64`

- **Form** — `neg64 dst`, i.e. `Neg(W64, dst)`
- **Encoding** — opcode byte `0x87` = `cls 0x07` + `opc 0x80` + `sbit 0x00`; `off = 0`; `imm = *`
- **Reads** — `dst` — the destination register, read as a 64-bit value
- **Writes** — `dst`, a 64-bit result

**Operation** — `dst = ((0 - dst) mod 2^64)`

`dst` is set to 0 minus the destination value, reduced modulo 2^64 (two's-complement wrap-around).

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Spec source** — `sem: wrap n ((0 - d))`, `defined: true` (`ebpf_alu.kspec:85:3`)

**Provenance** — RFC 9669 sec. 4.1 (NEG)

### family `mov` — `Ebpf.Ast.Mov`

- **AST constructor** — `Mov(w, dst, src:operand)`
- **Form axes** — `w : width`, `src : operand`
- **Widths** — `n = bits w`
- **Semantics signature** — `sem (n; s = src:operand@n) : bits n`
- **Definedness signature** — `defined (n)`
- **Instances** — 4
- **Cite** — RFC 9669 sec. 4.1 (MOV); CONSTRAINTS.md C10, C11

Width-generic behaviour, one row per table entry (this is what the F* backend
emits as a single `match`):

| entry | `sem` | `defined` | width-generic reading |
|---|---|---|---|
| `mov/MOV` | `s` | `true` | `dst` is set to the source operand value. |

#### `mov32 dst, src` — `mov/MOV/W32/reg`

- **Form** — `mov32 dst, src`, i.e. `Mov(W32, dst, reg)`
- **Encoding** — opcode byte `0xbc` = `cls 0x04` + `opc 0xb0` + `sbit 0x08`; `off = 0`; `imm = *`
- **Reads** — `src` — the source register, read as a 32-bit value (its low 32 bits)
- **Writes** — `dst`, a 32-bit result; writing a 32-bit value into the 64-bit destination register clears bits 32..63 (zero extension)

**Operation** — `dst = src`

`dst` is set to the source register value.

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Spec source** — `sem: s`, `defined: true` (`ebpf_alu.kspec:101:3`)

**Provenance** — RFC 9669 sec. 4.1 (MOV); CONSTRAINTS.md C10, C11

#### `mov32 dst, imm` — `mov/MOV/W32/imm`

- **Form** — `mov32 dst, imm`, i.e. `Mov(W32, dst, imm)`
- **Encoding** — opcode byte `0xb4` = `cls 0x04` + `opc 0xb0` + `sbit 0x00`; `off = 0`; `imm = *`
- **Reads** — `imm` — the 32-bit immediate field, read as a 32-bit operand value (see §1.4)
- **Writes** — `dst`, a 32-bit result; writing a 32-bit value into the 64-bit destination register clears bits 32..63 (zero extension)

**Operation** — `dst = imm`

`dst` is set to the immediate value.

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Spec source** — `sem: s`, `defined: true` (`ebpf_alu.kspec:101:3`)

**Provenance** — RFC 9669 sec. 4.1 (MOV); CONSTRAINTS.md C10, C11

#### `mov64 dst, src` — `mov/MOV/W64/reg`

- **Form** — `mov64 dst, src`, i.e. `Mov(W64, dst, reg)`
- **Encoding** — opcode byte `0xbf` = `cls 0x07` + `opc 0xb0` + `sbit 0x08`; `off = 0`; `imm = *`
- **Reads** — `src` — the source register, read as a 64-bit value
- **Writes** — `dst`, a 64-bit result

**Operation** — `dst = src`

`dst` is set to the source register value.

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Spec source** — `sem: s`, `defined: true` (`ebpf_alu.kspec:101:3`)

**Provenance** — RFC 9669 sec. 4.1 (MOV); CONSTRAINTS.md C10, C11

#### `mov64 dst, imm` — `mov/MOV/W64/imm`

- **Form** — `mov64 dst, imm`, i.e. `Mov(W64, dst, imm)`
- **Encoding** — opcode byte `0xb7` = `cls 0x07` + `opc 0xb0` + `sbit 0x00`; `off = 0`; `imm = *`
- **Reads** — `imm` — the 32-bit immediate field, read as a 64-bit operand value (see §1.4)
- **Writes** — `dst`, a 64-bit result

**Operation** — `dst = imm`

`dst` is set to the immediate value.

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Spec source** — `sem: s`, `defined: true` (`ebpf_alu.kspec:101:3`)

**Provenance** — RFC 9669 sec. 4.1 (MOV); CONSTRAINTS.md C10, C11

### family `movsx` — `Ebpf.Ast.MovSX`

- **AST constructor** — `MovSX(w, sz, dst, src:reg)`
- **Form axes** — `w : width`, `sz : movsx_sz`
- **Widths** — `n = bits w`, `f = bits sz`
- **Semantics signature** — `sem (n, f; s = src:reg@n) : bits n`
- **Definedness signature** — `defined (n, f)`
- **Instances** — 5
- **Validity** — `f < n`: f is less than n. Axis points that fail it are not instructions.
- **Excluded form** — `w = W32, sz = SX32` — off=32 is an ALU64-only MOVSX form (RFC 9669 sec. 4.1); the kernel rejects BPF_ALU|BPF_MOV|BPF_X with off=32
- **Cite** — RFC 9669 sec. 4.1 (MOVSX, ISA v4); CONSTRAINTS.md C13

Width-generic behaviour, one row per table entry (this is what the F* backend
emits as a single `match`):

| entry | `sem` | `defined` | width-generic reading |
|---|---|---|---|
| `movsx/MOVSX` | `sext f n (s)` | `true` | `dst` is set to the low f bits of the source register value, sign-extended to n bits. |

#### `movsx8_32 dst, src` — `movsx/MOVSX/W32/SX8`

- **Form** — `movsx8_32 dst, src`, i.e. `MovSX(W32, SX8, dst, src)`
- **Encoding** — opcode byte `0xbc` = `cls 0x04` + `opc 0xb0` + `sbit 0x08`; `off = 8`; `imm = *`
- **Reads** — `src` — the source register, read as a 32-bit value (its low 32 bits)
- **Writes** — `dst`, a 32-bit result; writing a 32-bit value into the 64-bit destination register clears bits 32..63 (zero extension)

**Operation** — `dst = sext(8, 32, src)`

`dst` is set to the low 8 bits of the source register value, sign-extended to 32 bits.

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Side conditions** (recorded in the IL, discharged on the generated F*):

- `f <= n` — the source width 8 is at most the target width 32 (`sext`)

**Spec source** — `sem: sext f n (s)`, `defined: true` (`ebpf_alu.kspec:121:3`)

**Provenance** — RFC 9669 sec. 4.1 (MOVSX, ISA v4); CONSTRAINTS.md C13

#### `movsx16_32 dst, src` — `movsx/MOVSX/W32/SX16`

- **Form** — `movsx16_32 dst, src`, i.e. `MovSX(W32, SX16, dst, src)`
- **Encoding** — opcode byte `0xbc` = `cls 0x04` + `opc 0xb0` + `sbit 0x08`; `off = 16`; `imm = *`
- **Reads** — `src` — the source register, read as a 32-bit value (its low 32 bits)
- **Writes** — `dst`, a 32-bit result; writing a 32-bit value into the 64-bit destination register clears bits 32..63 (zero extension)

**Operation** — `dst = sext(16, 32, src)`

`dst` is set to the low 16 bits of the source register value, sign-extended to 32 bits.

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Side conditions** (recorded in the IL, discharged on the generated F*):

- `f <= n` — the source width 16 is at most the target width 32 (`sext`)

**Spec source** — `sem: sext f n (s)`, `defined: true` (`ebpf_alu.kspec:121:3`)

**Provenance** — RFC 9669 sec. 4.1 (MOVSX, ISA v4); CONSTRAINTS.md C13

#### `movsx8_64 dst, src` — `movsx/MOVSX/W64/SX8`

- **Form** — `movsx8_64 dst, src`, i.e. `MovSX(W64, SX8, dst, src)`
- **Encoding** — opcode byte `0xbf` = `cls 0x07` + `opc 0xb0` + `sbit 0x08`; `off = 8`; `imm = *`
- **Reads** — `src` — the source register, read as a 64-bit value
- **Writes** — `dst`, a 64-bit result

**Operation** — `dst = sext(8, 64, src)`

`dst` is set to the low 8 bits of the source register value, sign-extended to 64 bits.

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Side conditions** (recorded in the IL, discharged on the generated F*):

- `f <= n` — the source width 8 is at most the target width 64 (`sext`)

**Spec source** — `sem: sext f n (s)`, `defined: true` (`ebpf_alu.kspec:121:3`)

**Provenance** — RFC 9669 sec. 4.1 (MOVSX, ISA v4); CONSTRAINTS.md C13

#### `movsx16_64 dst, src` — `movsx/MOVSX/W64/SX16`

- **Form** — `movsx16_64 dst, src`, i.e. `MovSX(W64, SX16, dst, src)`
- **Encoding** — opcode byte `0xbf` = `cls 0x07` + `opc 0xb0` + `sbit 0x08`; `off = 16`; `imm = *`
- **Reads** — `src` — the source register, read as a 64-bit value
- **Writes** — `dst`, a 64-bit result

**Operation** — `dst = sext(16, 64, src)`

`dst` is set to the low 16 bits of the source register value, sign-extended to 64 bits.

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Side conditions** (recorded in the IL, discharged on the generated F*):

- `f <= n` — the source width 16 is at most the target width 64 (`sext`)

**Spec source** — `sem: sext f n (s)`, `defined: true` (`ebpf_alu.kspec:121:3`)

**Provenance** — RFC 9669 sec. 4.1 (MOVSX, ISA v4); CONSTRAINTS.md C13

#### `movsx32_64 dst, src` — `movsx/MOVSX/W64/SX32`

- **Form** — `movsx32_64 dst, src`, i.e. `MovSX(W64, SX32, dst, src)`
- **Encoding** — opcode byte `0xbf` = `cls 0x07` + `opc 0xb0` + `sbit 0x08`; `off = 32`; `imm = *`
- **Reads** — `src` — the source register, read as a 64-bit value
- **Writes** — `dst`, a 64-bit result

**Operation** — `dst = sext(32, 64, src)`

`dst` is set to the low 32 bits of the source register value, sign-extended to 64 bits.

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Side conditions** (recorded in the IL, discharged on the generated F*):

- `f <= n` — the source width 32 is at most the target width 64 (`sext`)

**Spec source** — `sem: sext f n (s)`, `defined: true` (`ebpf_alu.kspec:121:3`)

**Provenance** — RFC 9669 sec. 4.1 (MOVSX, ISA v4); CONSTRAINTS.md C13

### family `swap` — `Ebpf.Ast.Swap`

- **AST constructor** — `Swap(k, sz, dst)`
- **Key** — `k : swap_kind` (one table row per case)
- **Form axes** — `sz : swap_sz`
- **Widths** — `m = bits sz`
- **Semantics signature** — `sem (m; d = dst@64) : bits m`
- **Definedness signature** — `defined (m)`
- **Instances** — 9
- **Host endianness** — the semantics below is stated for a **little-endian** host (`host` in the spec source). The pin is load-bearing: the `sem` cells of this family are the right ones only for that host.
- **Cite** — RFC 9669 sec. 4.1 (byte swap); CONSTRAINTS.md C14

Width-generic behaviour, one row per table entry (this is what the F* backend
emits as a single `match`):

| entry | `sem` | `defined` | width-generic reading |
|---|---|---|---|
| `swap/ToLE` | `low m (d)` | `true` | `dst` is set to the low m bits of the destination value. |
| `swap/ToBE` | `bswap (m / 8) (d)` | `true` | `dst` is set to the low (m / 8) bytes of the destination value in reverse order. |
| `swap/Bswap` | `bswap (m / 8) (d)` | `true` | `dst` is set to the low (m / 8) bytes of the destination value in reverse order. |

#### `le16 dst` — `swap/ToLE/SW16`

- **Form** — `le16 dst`, i.e. `Swap(ToLE, SW16, dst)`
- **Encoding** — opcode byte `0xd4` = `cls 0x04` + `opc 0xd0` + `sbit 0x00`; `off = 0`; `imm = 16`
- **Reads** — `dst` — the destination register, read as a 64-bit value
- **Writes** — `dst`, a 16-bit result; writing a 16-bit value into the 64-bit destination register clears bits 16..63 (zero extension)

**Operation** — `dst = (dst mod 2^16)`

`dst` is set to the low 16 bits of the destination value, on the pinned little-endian host (see the family note above).

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Spec source** — `sem: low m (d)`, `defined: true` (`ebpf_alu.kspec:140:3`)

**Provenance** — RFC 9669 sec. 4.1 (byte swap); CONSTRAINTS.md C14

#### `le32 dst` — `swap/ToLE/SW32`

- **Form** — `le32 dst`, i.e. `Swap(ToLE, SW32, dst)`
- **Encoding** — opcode byte `0xd4` = `cls 0x04` + `opc 0xd0` + `sbit 0x00`; `off = 0`; `imm = 32`
- **Reads** — `dst` — the destination register, read as a 64-bit value
- **Writes** — `dst`, a 32-bit result; writing a 32-bit value into the 64-bit destination register clears bits 32..63 (zero extension)

**Operation** — `dst = (dst mod 2^32)`

`dst` is set to the low 32 bits of the destination value, on the pinned little-endian host (see the family note above).

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Spec source** — `sem: low m (d)`, `defined: true` (`ebpf_alu.kspec:140:3`)

**Provenance** — RFC 9669 sec. 4.1 (byte swap); CONSTRAINTS.md C14

#### `le64 dst` — `swap/ToLE/SW64`

- **Form** — `le64 dst`, i.e. `Swap(ToLE, SW64, dst)`
- **Encoding** — opcode byte `0xd4` = `cls 0x04` + `opc 0xd0` + `sbit 0x00`; `off = 0`; `imm = 64`
- **Reads** — `dst` — the destination register, read as a 64-bit value
- **Writes** — `dst`, a 64-bit result

**Operation** — `dst = (dst mod 2^64)`

`dst` is set to the low 64 bits of the destination value, on the pinned little-endian host (see the family note above).

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Spec source** — `sem: low m (d)`, `defined: true` (`ebpf_alu.kspec:140:3`)

**Provenance** — RFC 9669 sec. 4.1 (byte swap); CONSTRAINTS.md C14

#### `be16 dst` — `swap/ToBE/SW16`

- **Form** — `be16 dst`, i.e. `Swap(ToBE, SW16, dst)`
- **Encoding** — opcode byte `0xdc` = `cls 0x04` + `opc 0xd0` + `sbit 0x08`; `off = 0`; `imm = 16`
- **Reads** — `dst` — the destination register, read as a 64-bit value
- **Writes** — `dst`, a 16-bit result; writing a 16-bit value into the 64-bit destination register clears bits 16..63 (zero extension)

**Operation** — `dst = bswap(2, dst)`

`dst` is set to the low 2 bytes of the destination value in reverse order, on the pinned little-endian host (see the family note above).

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Side conditions** (recorded in the IL, discharged on the generated F*):

- `8 | m` — 8 divides the result width 16 (`bswap` produces whole bytes)

**Spec source** — `sem: bswap (m / 8) (d)`, `defined: true` (`ebpf_alu.kspec:141:3`)

**Provenance** — RFC 9669 sec. 4.1 (byte swap); CONSTRAINTS.md C14

#### `be32 dst` — `swap/ToBE/SW32`

- **Form** — `be32 dst`, i.e. `Swap(ToBE, SW32, dst)`
- **Encoding** — opcode byte `0xdc` = `cls 0x04` + `opc 0xd0` + `sbit 0x08`; `off = 0`; `imm = 32`
- **Reads** — `dst` — the destination register, read as a 64-bit value
- **Writes** — `dst`, a 32-bit result; writing a 32-bit value into the 64-bit destination register clears bits 32..63 (zero extension)

**Operation** — `dst = bswap(4, dst)`

`dst` is set to the low 4 bytes of the destination value in reverse order, on the pinned little-endian host (see the family note above).

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Side conditions** (recorded in the IL, discharged on the generated F*):

- `8 | m` — 8 divides the result width 32 (`bswap` produces whole bytes)

**Spec source** — `sem: bswap (m / 8) (d)`, `defined: true` (`ebpf_alu.kspec:141:3`)

**Provenance** — RFC 9669 sec. 4.1 (byte swap); CONSTRAINTS.md C14

#### `be64 dst` — `swap/ToBE/SW64`

- **Form** — `be64 dst`, i.e. `Swap(ToBE, SW64, dst)`
- **Encoding** — opcode byte `0xdc` = `cls 0x04` + `opc 0xd0` + `sbit 0x08`; `off = 0`; `imm = 64`
- **Reads** — `dst` — the destination register, read as a 64-bit value
- **Writes** — `dst`, a 64-bit result

**Operation** — `dst = bswap(8, dst)`

`dst` is set to the low 8 bytes of the destination value in reverse order, on the pinned little-endian host (see the family note above).

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Side conditions** (recorded in the IL, discharged on the generated F*):

- `8 | m` — 8 divides the result width 64 (`bswap` produces whole bytes)

**Spec source** — `sem: bswap (m / 8) (d)`, `defined: true` (`ebpf_alu.kspec:141:3`)

**Provenance** — RFC 9669 sec. 4.1 (byte swap); CONSTRAINTS.md C14

#### `bswap16 dst` — `swap/Bswap/SW16`

- **Form** — `bswap16 dst`, i.e. `Swap(Bswap, SW16, dst)`
- **Encoding** — opcode byte `0xd7` = `cls 0x07` + `opc 0xd0` + `sbit 0x00`; `off = 0`; `imm = 16`
- **Reads** — `dst` — the destination register, read as a 64-bit value
- **Writes** — `dst`, a 16-bit result; writing a 16-bit value into the 64-bit destination register clears bits 16..63 (zero extension)

**Operation** — `dst = bswap(2, dst)`

`dst` is set to the low 2 bytes of the destination value in reverse order, on the pinned little-endian host (see the family note above).

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Side conditions** (recorded in the IL, discharged on the generated F*):

- `8 | m` — 8 divides the result width 16 (`bswap` produces whole bytes)

**Spec source** — `sem: bswap (m / 8) (d)`, `defined: true` (`ebpf_alu.kspec:142:3`)

**Provenance** — RFC 9669 sec. 4.1 (byte swap); CONSTRAINTS.md C14

#### `bswap32 dst` — `swap/Bswap/SW32`

- **Form** — `bswap32 dst`, i.e. `Swap(Bswap, SW32, dst)`
- **Encoding** — opcode byte `0xd7` = `cls 0x07` + `opc 0xd0` + `sbit 0x00`; `off = 0`; `imm = 32`
- **Reads** — `dst` — the destination register, read as a 64-bit value
- **Writes** — `dst`, a 32-bit result; writing a 32-bit value into the 64-bit destination register clears bits 32..63 (zero extension)

**Operation** — `dst = bswap(4, dst)`

`dst` is set to the low 4 bytes of the destination value in reverse order, on the pinned little-endian host (see the family note above).

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Side conditions** (recorded in the IL, discharged on the generated F*):

- `8 | m` — 8 divides the result width 32 (`bswap` produces whole bytes)

**Spec source** — `sem: bswap (m / 8) (d)`, `defined: true` (`ebpf_alu.kspec:142:3`)

**Provenance** — RFC 9669 sec. 4.1 (byte swap); CONSTRAINTS.md C14

#### `bswap64 dst` — `swap/Bswap/SW64`

- **Form** — `bswap64 dst`, i.e. `Swap(Bswap, SW64, dst)`
- **Encoding** — opcode byte `0xd7` = `cls 0x07` + `opc 0xd0` + `sbit 0x00`; `off = 0`; `imm = 64`
- **Reads** — `dst` — the destination register, read as a 64-bit value
- **Writes** — `dst`, a 64-bit result

**Operation** — `dst = bswap(8, dst)`

`dst` is set to the low 8 bytes of the destination value in reverse order, on the pinned little-endian host (see the family note above).

**Definedness** — `defined: true`. No precondition: the operation above is total over all operand values, in both checker modes.

**Side conditions** (recorded in the IL, discharged on the generated F*):

- `8 | m` — 8 divides the result width 64 (`bswap` produces whole bytes)

**Spec source** — `sem: bswap (m / 8) (d)`, `defined: true` (`ebpf_alu.kspec:142:3`)

**Provenance** — RFC 9669 sec. 4.1 (byte swap); CONSTRAINTS.md C14

## 3. Encoding summary

Every instance of the fragment, in generation order. `opcode` is the first byte (`cls + opc + sbit`); `*` marks a field the spec does not fix (§1.6). K8 (encoding disjointness) has checked that no two rows below can decode to the same bytes.

| id | mnemonic | opcode | cls | opc | sbit | off | imm |
|---|---|---|---|---|---|---|---|
| `alu/ADD/W32/reg` | `add32 dst, src` | `0x0c` | `0x04` | `0x00` | `0x08` | `0x00` | `*` |
| `alu/ADD/W32/imm` | `add32 dst, imm` | `0x04` | `0x04` | `0x00` | `0x00` | `0x00` | `*` |
| `alu/ADD/W64/reg` | `add64 dst, src` | `0x0f` | `0x07` | `0x00` | `0x08` | `0x00` | `*` |
| `alu/ADD/W64/imm` | `add64 dst, imm` | `0x07` | `0x07` | `0x00` | `0x00` | `0x00` | `*` |
| `alu/SUB/W32/reg` | `sub32 dst, src` | `0x1c` | `0x04` | `0x10` | `0x08` | `0x00` | `*` |
| `alu/SUB/W32/imm` | `sub32 dst, imm` | `0x14` | `0x04` | `0x10` | `0x00` | `0x00` | `*` |
| `alu/SUB/W64/reg` | `sub64 dst, src` | `0x1f` | `0x07` | `0x10` | `0x08` | `0x00` | `*` |
| `alu/SUB/W64/imm` | `sub64 dst, imm` | `0x17` | `0x07` | `0x10` | `0x00` | `0x00` | `*` |
| `alu/MUL/W32/reg` | `mul32 dst, src` | `0x2c` | `0x04` | `0x20` | `0x08` | `0x00` | `*` |
| `alu/MUL/W32/imm` | `mul32 dst, imm` | `0x24` | `0x04` | `0x20` | `0x00` | `0x00` | `*` |
| `alu/MUL/W64/reg` | `mul64 dst, src` | `0x2f` | `0x07` | `0x20` | `0x08` | `0x00` | `*` |
| `alu/MUL/W64/imm` | `mul64 dst, imm` | `0x27` | `0x07` | `0x20` | `0x00` | `0x00` | `*` |
| `alu/DIV/W32/reg` | `div32 dst, src` | `0x3c` | `0x04` | `0x30` | `0x08` | `0x00` | `*` |
| `alu/DIV/W32/imm` | `div32 dst, imm` | `0x34` | `0x04` | `0x30` | `0x00` | `0x00` | `*` |
| `alu/DIV/W64/reg` | `div64 dst, src` | `0x3f` | `0x07` | `0x30` | `0x08` | `0x00` | `*` |
| `alu/DIV/W64/imm` | `div64 dst, imm` | `0x37` | `0x07` | `0x30` | `0x00` | `0x00` | `*` |
| `alu/SDIV/W32/reg` | `sdiv32 dst, src` | `0x3c` | `0x04` | `0x30` | `0x08` | `0x01` | `*` |
| `alu/SDIV/W32/imm` | `sdiv32 dst, imm` | `0x34` | `0x04` | `0x30` | `0x00` | `0x01` | `*` |
| `alu/SDIV/W64/reg` | `sdiv64 dst, src` | `0x3f` | `0x07` | `0x30` | `0x08` | `0x01` | `*` |
| `alu/SDIV/W64/imm` | `sdiv64 dst, imm` | `0x37` | `0x07` | `0x30` | `0x00` | `0x01` | `*` |
| `alu/MOD/W32/reg` | `mod32 dst, src` | `0x9c` | `0x04` | `0x90` | `0x08` | `0x00` | `*` |
| `alu/MOD/W32/imm` | `mod32 dst, imm` | `0x94` | `0x04` | `0x90` | `0x00` | `0x00` | `*` |
| `alu/MOD/W64/reg` | `mod64 dst, src` | `0x9f` | `0x07` | `0x90` | `0x08` | `0x00` | `*` |
| `alu/MOD/W64/imm` | `mod64 dst, imm` | `0x97` | `0x07` | `0x90` | `0x00` | `0x00` | `*` |
| `alu/SMOD/W32/reg` | `smod32 dst, src` | `0x9c` | `0x04` | `0x90` | `0x08` | `0x01` | `*` |
| `alu/SMOD/W32/imm` | `smod32 dst, imm` | `0x94` | `0x04` | `0x90` | `0x00` | `0x01` | `*` |
| `alu/SMOD/W64/reg` | `smod64 dst, src` | `0x9f` | `0x07` | `0x90` | `0x08` | `0x01` | `*` |
| `alu/SMOD/W64/imm` | `smod64 dst, imm` | `0x97` | `0x07` | `0x90` | `0x00` | `0x01` | `*` |
| `alu/AND/W32/reg` | `and32 dst, src` | `0x5c` | `0x04` | `0x50` | `0x08` | `0x00` | `*` |
| `alu/AND/W32/imm` | `and32 dst, imm` | `0x54` | `0x04` | `0x50` | `0x00` | `0x00` | `*` |
| `alu/AND/W64/reg` | `and64 dst, src` | `0x5f` | `0x07` | `0x50` | `0x08` | `0x00` | `*` |
| `alu/AND/W64/imm` | `and64 dst, imm` | `0x57` | `0x07` | `0x50` | `0x00` | `0x00` | `*` |
| `alu/OR/W32/reg` | `or32 dst, src` | `0x4c` | `0x04` | `0x40` | `0x08` | `0x00` | `*` |
| `alu/OR/W32/imm` | `or32 dst, imm` | `0x44` | `0x04` | `0x40` | `0x00` | `0x00` | `*` |
| `alu/OR/W64/reg` | `or64 dst, src` | `0x4f` | `0x07` | `0x40` | `0x08` | `0x00` | `*` |
| `alu/OR/W64/imm` | `or64 dst, imm` | `0x47` | `0x07` | `0x40` | `0x00` | `0x00` | `*` |
| `alu/XOR/W32/reg` | `xor32 dst, src` | `0xac` | `0x04` | `0xa0` | `0x08` | `0x00` | `*` |
| `alu/XOR/W32/imm` | `xor32 dst, imm` | `0xa4` | `0x04` | `0xa0` | `0x00` | `0x00` | `*` |
| `alu/XOR/W64/reg` | `xor64 dst, src` | `0xaf` | `0x07` | `0xa0` | `0x08` | `0x00` | `*` |
| `alu/XOR/W64/imm` | `xor64 dst, imm` | `0xa7` | `0x07` | `0xa0` | `0x00` | `0x00` | `*` |
| `alu/LSH/W32/reg` | `lsh32 dst, src` | `0x6c` | `0x04` | `0x60` | `0x08` | `0x00` | `*` |
| `alu/LSH/W32/imm` | `lsh32 dst, imm` | `0x64` | `0x04` | `0x60` | `0x00` | `0x00` | `*` |
| `alu/LSH/W64/reg` | `lsh64 dst, src` | `0x6f` | `0x07` | `0x60` | `0x08` | `0x00` | `*` |
| `alu/LSH/W64/imm` | `lsh64 dst, imm` | `0x67` | `0x07` | `0x60` | `0x00` | `0x00` | `*` |
| `alu/RSH/W32/reg` | `rsh32 dst, src` | `0x7c` | `0x04` | `0x70` | `0x08` | `0x00` | `*` |
| `alu/RSH/W32/imm` | `rsh32 dst, imm` | `0x74` | `0x04` | `0x70` | `0x00` | `0x00` | `*` |
| `alu/RSH/W64/reg` | `rsh64 dst, src` | `0x7f` | `0x07` | `0x70` | `0x08` | `0x00` | `*` |
| `alu/RSH/W64/imm` | `rsh64 dst, imm` | `0x77` | `0x07` | `0x70` | `0x00` | `0x00` | `*` |
| `alu/ARSH/W32/reg` | `arsh32 dst, src` | `0xcc` | `0x04` | `0xc0` | `0x08` | `0x00` | `*` |
| `alu/ARSH/W32/imm` | `arsh32 dst, imm` | `0xc4` | `0x04` | `0xc0` | `0x00` | `0x00` | `*` |
| `alu/ARSH/W64/reg` | `arsh64 dst, src` | `0xcf` | `0x07` | `0xc0` | `0x08` | `0x00` | `*` |
| `alu/ARSH/W64/imm` | `arsh64 dst, imm` | `0xc7` | `0x07` | `0xc0` | `0x00` | `0x00` | `*` |
| `neg/NEG/W32` | `neg32 dst` | `0x84` | `0x04` | `0x80` | `0x00` | `0x00` | `*` |
| `neg/NEG/W64` | `neg64 dst` | `0x87` | `0x07` | `0x80` | `0x00` | `0x00` | `*` |
| `mov/MOV/W32/reg` | `mov32 dst, src` | `0xbc` | `0x04` | `0xb0` | `0x08` | `0x00` | `*` |
| `mov/MOV/W32/imm` | `mov32 dst, imm` | `0xb4` | `0x04` | `0xb0` | `0x00` | `0x00` | `*` |
| `mov/MOV/W64/reg` | `mov64 dst, src` | `0xbf` | `0x07` | `0xb0` | `0x08` | `0x00` | `*` |
| `mov/MOV/W64/imm` | `mov64 dst, imm` | `0xb7` | `0x07` | `0xb0` | `0x00` | `0x00` | `*` |
| `movsx/MOVSX/W32/SX8` | `movsx8_32 dst, src` | `0xbc` | `0x04` | `0xb0` | `0x08` | `0x08` | `*` |
| `movsx/MOVSX/W32/SX16` | `movsx16_32 dst, src` | `0xbc` | `0x04` | `0xb0` | `0x08` | `0x10` | `*` |
| `movsx/MOVSX/W64/SX8` | `movsx8_64 dst, src` | `0xbf` | `0x07` | `0xb0` | `0x08` | `0x08` | `*` |
| `movsx/MOVSX/W64/SX16` | `movsx16_64 dst, src` | `0xbf` | `0x07` | `0xb0` | `0x08` | `0x10` | `*` |
| `movsx/MOVSX/W64/SX32` | `movsx32_64 dst, src` | `0xbf` | `0x07` | `0xb0` | `0x08` | `0x20` | `*` |
| `swap/ToLE/SW16` | `le16 dst` | `0xd4` | `0x04` | `0xd0` | `0x00` | `0x00` | `0x10` |
| `swap/ToLE/SW32` | `le32 dst` | `0xd4` | `0x04` | `0xd0` | `0x00` | `0x00` | `0x20` |
| `swap/ToLE/SW64` | `le64 dst` | `0xd4` | `0x04` | `0xd0` | `0x00` | `0x00` | `0x40` |
| `swap/ToBE/SW16` | `be16 dst` | `0xdc` | `0x04` | `0xd0` | `0x08` | `0x00` | `0x10` |
| `swap/ToBE/SW32` | `be32 dst` | `0xdc` | `0x04` | `0xd0` | `0x08` | `0x00` | `0x20` |
| `swap/ToBE/SW64` | `be64 dst` | `0xdc` | `0x04` | `0xd0` | `0x08` | `0x00` | `0x40` |
| `swap/Bswap/SW16` | `bswap16 dst` | `0xd7` | `0x07` | `0xd0` | `0x00` | `0x00` | `0x10` |
| `swap/Bswap/SW32` | `bswap32 dst` | `0xd7` | `0x07` | `0xd0` | `0x00` | `0x00` | `0x20` |
| `swap/Bswap/SW64` | `bswap64 dst` | `0xd7` | `0x07` | `0xd0` | `0x00` | `0x00` | `0x40` |

## 4. Forms that are not instructions

Axis points that the spec claims are *not* instructions. Each one is a load-bearing claim: it costs a `valid` predicate that agrees with it (checked by K6) and a mandatory reason.

| family | excluded point | `valid` predicate | reason |
|---|---|---|---|
| `movsx` | `w = W32, sz = SX32` | `f < n` | off=32 is an ALU64-only MOVSX form (RFC 9669 sec. 4.1); the kernel rejects BPF_ALU\|BPF_MOV\|BPF_X with off=32 |

## 5. Coverage

One section per instruction of the fragment: 72 sections for 72 instances, from 19 table entries in 5 families, plus 1 excluded form listed in §4. `spec/tests/run.sh` re-generates this file and fails if it differs from the committed copy, and separately asserts that every entry id and every instance id in the IL occurs here — a silently omitted instruction is a test failure.

